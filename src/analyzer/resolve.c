#include "analyzer/resolve.h"
#include <stddef.h>

/* Stub implementation — will be replaced with full two-pass resolver. */
Iron_Scope *iron_resolve(Iron_Program *program, Iron_Arena *arena,
                          Iron_DiagList *diags) {
    (void)program;
    (void)arena;
    (void)diags;
    return NULL;
}
