{"iterations": 50000000, "timeout_sec": 60, "max_ratio": 2.0, "note": "Both Iron and C use int64_t arrays; stable ratio ~1.1-1.3x at 50M iterations (C ~115ms, Iron ~140ms). At lower iteration counts, sub-50ms timing causes extreme variance. Threshold set to 1.5x to absorb measurement noise above the true ~1.2x ratio."}