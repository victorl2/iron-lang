{"iterations": 1000000, "timeout_sec": 60, "max_ratio": 8.0, "note": "Both Iron and C use int32_t. Local ~4.2x. Remaining gap is goto control flow (prevents clang vectorization) + array length params. CI headroom for Ubuntu clang differences."}