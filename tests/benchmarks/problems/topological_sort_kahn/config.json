{"iterations": 2000000, "timeout_sec": 60, "max_ratio": 1.7, "note": "Iron fill(20, 0) heap-allocates int64_t in_deg and queue arrays vs C int in_deg[20]/queue[20] stack arrays (int32). Language-design-inherent: Iron Int=int64_t and fill() heap-allocates. Stable observed ratio ~1.4x at C=342ms; threshold set to 1.7x (+20% margin on 1.4x)."}
