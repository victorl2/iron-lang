{"iterations": 1, "timeout_sec": 120, "max_ratio": 2.0, "note": "Iron spawns 8 background threads + main thread in parallel; C spawns 8 pthreads + main. Both do equivalent parallel work. C time ~23ms (sub-50ms timing noise). Stable observed ratio ~0.7x post-optimization; threshold 2.0x absorbs full-suite thread scheduling variance and sub-50ms measurement noise."}