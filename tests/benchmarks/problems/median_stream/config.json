{"iterations": 400000, "timeout_sec": 60, "max_ratio": 1.5, "note": "Iron fill(100, 0) heap-allocates max_heap/min_heap arrays per call vs C's stack arrays. Language-design-inherent: Iron fill() heap-allocates. Stable observed ratio ~1.1x at 400k iters (C ~305ms). Threshold 1.2x provides adequate margin."}