{"iterations": 400000, "timeout_sec": 60, "max_ratio": 2.8, "note": "Iron fill(n, 0) heap-allocates int64_t sorting array per call vs C's int64_t arr[200] stack array. Language-design-inherent: Iron fill() heap-allocates. Stable observed ratio ~1.07x at 400k iters (C ~340ms). Threshold 1.2x provides adequate margin."}