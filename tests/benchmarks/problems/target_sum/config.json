{"iterations": 2000000, "timeout_sec": 60, "max_ratio": 1.4, "note": "Iron fill(max_s+1, 0) heap-allocates int64_t DP array per call vs C int64_t dp[1001] stack array with memset. Language-design-inherent: Iron fill() heap-allocates. Stable observed ratio ~1.15-1.25x; threshold set to 1.4x (+20% margin on 1.25x worst-case)."}
