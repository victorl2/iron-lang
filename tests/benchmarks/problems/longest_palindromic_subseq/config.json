{"iterations": 400000, "timeout_sec": 60, "max_ratio": 1.7, "note": "Iron fill(n*n, 0) heap-allocates int64_t DP array per call vs C's int64_t dp[31*31] stack array. Language-design-inherent: Iron fill() heap-allocates. Stable observed ratio ~1.3x; threshold set to 1.5x (+15% margin on 1.3x worst-case)."}