import time

-- Hamming Distance: count differing bits between two integers
-- Uses arithmetic (mod/div) since Iron lacks bitwise operators

func hamming(a: Int, b: Int) -> Int {
    var x = a
    var y = b
    var count = 0
    while x > 0 or y > 0 {
        if (x % 2) != (y % 2) {
            count = count + 1
        }
        x = x / 2
        y = y / 2
    }
    return count
}

func run_hamming_batch() -> Int {
    var total = 0
    for i in range(1000000) {
        val a = (i * 31 + 17) % 100000
        val b = (i * 37 + 23) % 100000
        total = total + hamming(a, b)
    }
    return total
}

func main() {
    println("Test 1: {hamming(1, 4)} (expected 2)")
    println("Test 2: {hamming(3, 1)} (expected 1)")
    println("Test 3: {hamming(0, 0)} (expected 0)")

    -- Correctness check: sum hamming distances for 1M pairs
    val check = run_hamming_batch()
    println("Test 4: {check} (expected 8217960)")

    -- Benchmark: repeat 10 times for stable C time > 200ms
    val iterations = 10
    val start_ns = Time.now_ns()
    var result = 0
    for it in range(iterations) {
        result = run_hamming_batch()
    }
    val elapsed_ns = Time.now_ns() - start_ns
    val elapsed_ms = elapsed_ns / 1000000

    println("")
    println("=== Benchmark: Hamming Distance ===")
    println("Pairs: 10000000")
    println("Total time: {elapsed_ns} ns")
    println("Total time: {elapsed_ms} ms")
}
