{"iterations": 300000, "timeout_sec": 60, "max_ratio": 1.2, "note": "Iron fill(2000, 0) heap-allocates int64_t data/mins arrays per call vs C's int64_t data[2000]/mins[2000] stack arrays. Language-design-inherent: Iron fill() heap-allocates. Stable observed ratio ~1.15x at 300k iters (C ~400ms). Threshold 1.2x provides adequate margin."}
