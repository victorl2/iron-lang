{"iterations": 2000000, "timeout_sec": 60, "max_ratio": 1.5, "note": "Iron fill(n*n, 0) and fill(n, 0) heap-allocate BFS color/queue arrays vs C int color[20]/queue[20] stack arrays. Language-design-inherent: Iron fill() heap-allocates. Stable observed ratio ~1.2x at 2M iters (C ~375ms). Threshold 1.2x with C >> 200ms provides stable measurement."}