-- coll_filter_basic.iron: Test arr.filter() keeping only elements matching predicate
func main() {
  val arr = [1, 2, 3, 4, 5, 6, 7, 8]
  val big = arr.filter(func(x: Int) -> Bool { return x > 5 })
  for v in big {
    println("{v}")
  }
}
