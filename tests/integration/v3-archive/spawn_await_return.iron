func compute(n: Int) -> Int {
  var s = 0
  var i = 0
  while i < n {
    s = s + i
    i = i + 1
  }
  return s
}

func main() {
  val h = spawn("worker") {
    return compute(10)
  }
  val r = await h
  println("{r}")
}
