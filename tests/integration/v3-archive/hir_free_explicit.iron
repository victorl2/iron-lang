object Node {
  var id: Int
  var label: String
    init(id: Int, label: String) {
        self.id = id
        self.label = label
    }
}

func main() {
  val n = heap Node(1, "first")
  println("{n.id}")
  println(n.label)
  free(n)
  println("freed ok")
}
