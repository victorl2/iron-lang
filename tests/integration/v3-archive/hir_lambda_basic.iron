func main() {
  val double = func(x: Int) -> Int {
    return x * 2
  }
  val square = func(x: Int) -> Int {
    return x * x
  }
  println("{double(5)}")
  println("{square(4)}")
  println("{double(square(3))}")
}
