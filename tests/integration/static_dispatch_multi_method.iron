-- static_dispatch_multi_method.iron: Interface with multiple methods

interface Vehicle {
  func speed() -> Int
  func fuel() -> Int
  func describe() -> String
}

object Car impl Vehicle {
  var max_speed: Int
  var tank: Int

    init(max_speed: Int, tank: Int) {
        self.max_speed = max_speed
        self.tank = tank
    }
}

object Bike impl Vehicle {
  var max_speed: Int

    init(max_speed: Int) {
        self.max_speed = max_speed
    }
}

patch object Car {
    readonly func speed() -> Int {
      return self.max_speed
    }
    readonly func fuel() -> Int {
      return self.tank
    }
    readonly func describe() -> String {
      return "Car(speed={self.max_speed}, fuel={self.tank})"
    }
}



patch object Bike {
    readonly func speed() -> Int {
      return self.max_speed
    }
    readonly func fuel() -> Int {
      return 0
    }
    readonly func describe() -> String {
      return "Bike(speed={self.max_speed})"
    }
}



func report(v: Vehicle) {
  println("{v.describe()} goes {v.speed()} with {v.fuel()} fuel")
}

func main() {
  report(Car(120, 50))
  report(Bike(25))
  report(Car(200, 80))

  println("done")
}
