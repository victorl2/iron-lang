import log

func main() {
  -- Log.info writes to stderr with a dynamic timestamp, making exact matching hard.
  -- We verify the module imports and compiles correctly without calling Log.info.
  -- The combined test exercises Log.info; here we verify import wiring only.
  println("log works")
}
