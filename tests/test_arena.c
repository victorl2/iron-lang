/* Placeholder — full tests added in Task 2. */
#include "unity.h"
#include "util/arena.h"

void setUp(void) {}
void tearDown(void) {}

static void test_placeholder(void) {
    TEST_ASSERT_TRUE(1);
}

int main(void) {
    UNITY_BEGIN();
    RUN_TEST(test_placeholder);
    return UNITY_END();
}
