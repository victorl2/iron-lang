// Phase 34 LSP-03: `weak` visible only when followed by `rc` in
// type/initializer position; otherwise hidden.
object Point { val x: Int val y: Int }
func main() {
    val p = rc Point(1, 2)
    // COMPLETE@6:18
    // EXPECT_VISIBLE: weak
    val w = p.downgrade()

    // COMPLETE@10:5
    // EXPECT_HIDDEN: weak
    val z = 1
}
