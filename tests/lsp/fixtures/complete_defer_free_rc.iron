// Phase 34 LSP-04: `defer free <binding>` snippet offered after a recent
// `rc` binding.
object Point { val x: Int val y: Int }
func main() {
    val shared = rc Point(1, 2)
    // COMPLETE@6:5
    // EXPECT_SNIPPET: defer free ${1:shared}

}
