func compute(n: Int) -> Int {
  var sum = 0
  var i = 0
  while (i < n) {
    sum = (sum + i)
    i = (i + 1)
  }
  return sum
}

func main() {
  val h = spawn("worker") {
    return compute(10)
  }
  val r = await h
  println("result: {r}")
}
