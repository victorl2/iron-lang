import math

func main() {
  val x = 42
  println("value is {x}")
  val pi = Math.sin(0.0)
  println("sin(0) is 0")
  val root = Math.sqrt(16.0)
  println("sqrt(16) = {root}")
  for i in 10 parallel {
  }
  println("all features work")
}
