object Point {
  var x: Int
  var y: Int
  init(x: Int, y: Int) {
    self.x = x
    self.y = y
  }
}

func main() {
  val p = heap Point(3, 4)
  println("{p.x}")
  println("{p.y}")
  val sum = (p.x + p.y)
  println("{sum}")
}
