enum Option {
  Some(T),
  None,
}

enum Result {
  Ok(T),
  Err(E),
}

func main() {
  val inner: Option[Int] = Option.Some(99)
  val outer: Result[Option[Int], String] = Result.Ok(inner)
  println("nested: ok")
}
