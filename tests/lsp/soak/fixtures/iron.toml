[package]
name = "soak_workspace"
version = "0.1.0"

# Phase 7 Plan 07-02 Task 02 (HARD-16, D-04) -- canonical soak workspace.
# 50 module_NN.iron files totalling ~5000 LoC with cross-references so the
# workspace index is continually exercised during the 8h soak. Generated by
# tests/lsp/soak/gen_fixtures.py; regenerating MUST produce byte-identical
# files (the soak harness thresholds are calibrated against this specific
# content).
