---
phase: 39-module-completions-math-io-time-log
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stdlib/io.iron
  - src/stdlib/iron_io.h
  - src/stdlib/iron_io.c
autonomous: true
requirements:
  - IO-01
  - IO-02
  - IO-03
  - IO-04
  - IO-05
  - IO-06
  - IO-07
  - IO-08
  - IO-09
  - IO-10

must_haves:
  truths:
    - "io.read_line() reads one line from stdin, returning empty string at EOF"
    - "io.append_file(path, content) appends to a file without truncating"
    - "io.basename('/a/b/c.txt') returns 'c.txt'"
    - "io.dirname('/a/b/c.txt') returns '/a/b'"
    - "io.join_path('a', 'b') returns 'a/b' using forward slash"
    - "io.extension('file.iron') returns 'iron' (without the dot)"
    - "io.is_dir(path) returns correct Bool via stat()"
    - "io.read_lines(path) returns List[String] with one element per line, no trailing empty element"
    - "IO-01 (read_bytes) and IO-02 (write_bytes) are deferred — not implemented, not declared in io.iron"
  artifacts:
    - path: "src/stdlib/io.iron"
      provides: "Iron API declarations for 8 new IO functions (IO-03..IO-10)"
      contains: "func IO.read_line"
    - path: "src/stdlib/iron_io.h"
      provides: "C prototypes for all 8 new IO functions"
      contains: "Iron_io_read_line"
    - path: "src/stdlib/iron_io.c"
      provides: "C implementations of all 8 new IO functions"
      contains: "Iron_io_read_lines"
  key_links:
    - from: "src/stdlib/io.iron"
      to: "src/stdlib/iron_io.c"
      via: "name mangling: IO.read_line -> Iron_io_read_line"
      pattern: "Iron_io_read_line"
    - from: "Iron_io_read_lines"
      to: "Iron_io_read_file_result + Iron_string_split"
      via: "read file content then split on newline"
      pattern: "Iron_string_split"
---

<objective>
Add 8 missing IO functions to the IO stdlib module (IO-03..IO-10). IO-01 and IO-02 (read_bytes/write_bytes) are deferred per user decision. Append new declarations to io.iron and iron_io.h, implement bodies in iron_io.c.

Purpose: Close the gap between language_definition.md's IO spec and the working implementation for all non-deferred functions.
Output: io.iron + iron_io.h + iron_io.c with read_line, append_file, basename, dirname, join_path, extension, is_dir, read_lines.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/victor/code/iron-lang/.planning/phases/39-module-completions-math-io-time-log/39-CONTEXT.md
@/Users/victor/code/iron-lang/.planning/phases/39-module-completions-math-io-time-log/39-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs. Read before touching any file. -->

From src/stdlib/io.iron (current — 10 lines):
```iron
-- io.iron -- Iron wrapper for the IO stdlib module
object IO {}

func IO.read_file(path: String) -> String {}
func IO.write_file(path: String, content: String) {}
func IO.file_exists(path: String) -> Bool {}
func IO.create_dir(path: String) {}
func IO.delete_file(path: String) {}
func IO.list_files(dir_path: String) -> String {}
```

From src/stdlib/iron_io.h (current):
```c
#include "runtime/iron_runtime.h"   // <- already included; Iron_List_Iron_String available
typedef struct { Iron_String v0; Iron_Error v1; } Iron_Result_String_Error;
typedef struct { bool v0; Iron_Error v1; } Iron_Result_bool_Error;
Iron_Result_String_Error Iron_io_read_file_result(Iron_String path);
Iron_Error               Iron_io_write_file(Iron_String path, Iron_String content);
bool       Iron_io_file_exists(Iron_String path);
Iron_Error Iron_io_create_dir(Iron_String path);
Iron_Error Iron_io_delete_file(Iron_String path);
Iron_Result_String_Error Iron_io_list_files_result(Iron_String dir_path);
static inline Iron_String Iron_io_read_file(Iron_String path) { ... }
static inline Iron_String Iron_io_list_files(Iron_String dir_path) { ... }
```

Runtime types available via iron_io.h -> iron_runtime.h:
```c
Iron_String iron_string_from_cstr(const char *s, size_t len);
const char* iron_string_cstr(const Iron_String *s);
size_t      iron_string_byte_len(const Iron_String *s);
Iron_List_Iron_String Iron_string_split(Iron_String s, Iron_String sep);
Iron_List_Iron_String Iron_List_Iron_String_create(void);
```

From src/stdlib/iron_io.c (current):
- Already includes: stdio.h, stdlib.h, string.h, sys/stat.h, dirent.h, errno.h
- Already defines: iron_error_new(), iron_error_none(), iron_string_cstr(), iron_string_byte_len()
  via iron_io.h -> iron_runtime.h

Name mangling:
  IO.read_line   -> Iron_io_read_line
  IO.append_file -> Iron_io_append_file
  IO.basename    -> Iron_io_basename
  IO.dirname     -> Iron_io_dirname
  IO.join_path   -> Iron_io_join_path
  IO.extension   -> Iron_io_extension
  IO.is_dir      -> Iron_io_is_dir
  IO.read_lines  -> Iron_io_read_lines

DEFERRED (IO-01, IO-02): read_bytes / write_bytes already have C stubs in iron_io.c and
header entries in iron_io.h but are NOT declared in io.iron. Do NOT add them to io.iron.

SPEC NOTE: io.extension("file.iron") must return "iron" (WITHOUT the dot).
The ROADMAP success criterion explicitly states this. The RESEARCH code example
returns the dot — that example is wrong. Strip the dot before returning.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 8 new IO function declarations to io.iron and iron_io.h</name>
  <files>src/stdlib/io.iron, src/stdlib/iron_io.h</files>
  <read_first>
    - src/stdlib/io.iron (full file — 10 lines)
    - src/stdlib/iron_io.h (full file — 37 lines)
  </read_first>
  <action>
Append the following 8 function declarations to `src/stdlib/io.iron` after the existing `func IO.list_files` line:

```iron
func IO.read_line() -> String {}
func IO.append_file(path: String, content: String) {}
func IO.basename(path: String) -> String {}
func IO.dirname(path: String) -> String {}
func IO.join_path(a: String, b: String) -> String {}
func IO.extension(path: String) -> String {}
func IO.is_dir(path: String) -> Bool {}
func IO.read_lines(path: String) -> List[String] {}
```

Do NOT add read_bytes or write_bytes — they are deferred (IO-01, IO-02).

Append the following to `src/stdlib/iron_io.h` before `#endif`:

```c
/* ── Phase 39 additions ──────────────────────────────────────────────────── */
Iron_String            Iron_io_read_line(void);
Iron_Error             Iron_io_append_file(Iron_String path, Iron_String content);
Iron_String            Iron_io_basename(Iron_String path);
Iron_String            Iron_io_dirname(Iron_String path);
Iron_String            Iron_io_join_path(Iron_String a, Iron_String b);
Iron_String            Iron_io_extension(Iron_String path);
bool                   Iron_io_is_dir(Iron_String path);
Iron_List_Iron_String  Iron_io_read_lines(Iron_String path);
```
  </action>
  <verify>
    <automated>grep -c "Iron_io_read_line\|Iron_io_append_file\|Iron_io_basename\|Iron_io_dirname\|Iron_io_join_path\|Iron_io_extension\|Iron_io_is_dir\|Iron_io_read_lines" /Users/victor/code/iron-lang/src/stdlib/iron_io.h</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func IO.read_line" src/stdlib/io.iron` returns `1`
    - `grep -c "func IO.read_lines" src/stdlib/io.iron` returns `1`
    - `grep "func IO.read_bytes" src/stdlib/io.iron` returns empty (deferred, must NOT be present)
    - `grep -c "Iron_io_read_lines" src/stdlib/iron_io.h` returns `1`
    - Count check above returns `8`
  </acceptance_criteria>
  <done>io.iron declares all 8 new non-deferred IO functions. iron_io.h declares all 8 new C prototypes. read_bytes/write_bytes absent from io.iron.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 8 new IO functions in iron_io.c</name>
  <files>src/stdlib/iron_io.c</files>
  <read_first>
    - src/stdlib/iron_io.c (full file — 178 lines)
    - src/stdlib/iron_io.h (to confirm Iron_List_Iron_String and existing function signatures)
    - src/runtime/iron_runtime.h (to confirm Iron_string_split signature)
  </read_first>
  <action>
Append the following block to the end of `src/stdlib/iron_io.c` (after the existing Iron_io_list_files_result function):

```c
/* ── Phase 39 additions ──────────────────────────────────────────────────── */

Iron_String Iron_io_read_line(void) {
    char buf[4096];
    if (!fgets(buf, sizeof(buf), stdin)) {
        return iron_string_from_cstr("", 0);  /* EOF — return empty string */
    }
    size_t len = strlen(buf);
    /* Strip trailing CR and LF */
    if (len > 0 && buf[len - 1] == '\n') { buf[--len] = '\0'; }
    if (len > 0 && buf[len - 1] == '\r') { buf[--len] = '\0'; }
    return iron_string_from_cstr(buf, len);
}

Iron_Error Iron_io_append_file(Iron_String path, Iron_String content) {
    const char *p = iron_string_cstr(&path);
    FILE *f = fopen(p, "ab");
    if (!f) return iron_error_new(1, "could not open file for appending");
    const char *data = iron_string_cstr(&content);
    size_t len = iron_string_byte_len(&content);
    size_t written = fwrite(data, 1, len, f);
    fclose(f);
    if (written != len) return iron_error_new(5, "write incomplete");
    return iron_error_none();
}

Iron_String Iron_io_basename(Iron_String path) {
    const char *p = iron_string_cstr(&path);
    const char *last = strrchr(p, '/');
    const char *base = last ? last + 1 : p;
    return iron_string_from_cstr(base, strlen(base));
}

Iron_String Iron_io_dirname(Iron_String path) {
    const char *p = iron_string_cstr(&path);
    const char *last = strrchr(p, '/');
    if (!last) return iron_string_from_cstr(".", 1);
    if (last == p) return iron_string_from_cstr("/", 1);
    size_t len = (size_t)(last - p);
    return iron_string_from_cstr(p, len);
}

Iron_String Iron_io_join_path(Iron_String a, Iron_String b) {
    /* Always forward slash — works on Windows too (locked decision) */
    const char *ap = iron_string_cstr(&a);
    const char *bp = iron_string_cstr(&b);
    size_t alen = iron_string_byte_len(&a);
    /* Strip trailing slash(es) from a */
    while (alen > 0 && ap[alen - 1] == '/') alen--;
    size_t blen = strlen(bp);
    size_t total = alen + 1 + blen + 1;
    char *buf = (char *)malloc(total);
    if (!buf) return iron_string_from_cstr("", 0);
    memcpy(buf, ap, alen);
    buf[alen] = '/';
    memcpy(buf + alen + 1, bp, blen);
    buf[alen + 1 + blen] = '\0';
    Iron_String result = iron_string_from_cstr(buf, alen + 1 + blen);
    free(buf);
    return result;
}

Iron_String Iron_io_extension(Iron_String path) {
    /* Returns extension WITHOUT the leading dot: "file.iron" -> "iron" */
    const char *p = iron_string_cstr(&path);
    const char *last_slash = strrchr(p, '/');
    const char *base = last_slash ? last_slash + 1 : p;
    const char *dot = strrchr(base, '.');
    /* No dot, or dot is the first char of filename (e.g. ".gitignore") */
    if (!dot || dot == base) return iron_string_from_cstr("", 0);
    /* Skip the dot — return extension only */
    const char *ext = dot + 1;
    return iron_string_from_cstr(ext, strlen(ext));
}

bool Iron_io_is_dir(Iron_String path) {
    const char *p = iron_string_cstr(&path);
    struct stat st;
    if (stat(p, &st) != 0) return false;
    return S_ISDIR(st.st_mode);
}

Iron_List_Iron_String Iron_io_read_lines(Iron_String path) {
    /* Read file content then split on newline. Strip trailing empty element. */
    Iron_Result_String_Error res = Iron_io_read_file_result(path);
    if (!iron_error_is_ok(res.v1)) {
        return Iron_List_Iron_String_create();
    }
    Iron_String sep = iron_string_from_cstr("\n", 1);
    Iron_List_Iron_String lines = Iron_string_split(res.v0, sep);
    /* Remove trailing empty string if file ended with \n (common case) */
    if (lines.count > 0) {
        Iron_String last = lines.items[lines.count - 1];
        if (iron_string_byte_len(&last) == 0) {
            lines.count--;
        }
    }
    return lines;
}
```

Note on `iron_error_is_ok`: verify this function exists in iron_runtime.h before using it. If it does not exist, use `res.v1.code == 0` instead. Read iron_runtime.h to confirm.
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang/build && cmake --build . -j4 --target ironc 2>&1 | grep -E "^.*error:" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Iron_io_read_line\|Iron_io_append_file\|Iron_io_basename\|Iron_io_dirname\|Iron_io_join_path\|Iron_io_extension\|Iron_io_is_dir\|Iron_io_read_lines" src/stdlib/iron_io.c` returns `8` (definitions, not just mentions)
    - `grep "WITHOUT the leading dot" src/stdlib/iron_io.c` confirms the extension stripping is present
    - Build command produces no error lines (empty output from grep -E "error:" filter)
  </acceptance_criteria>
  <done>iron_io.c implements all 8 new IO functions. io.extension returns without the dot. io.read_lines strips trailing empty element. Build compiles clean.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify with a quick end-to-end compilation:

```bash
cd /Users/victor/code/iron-lang && cat > /tmp/test_io39.iron << 'EOF'
import io

func main() {
  val b = IO.basename("/a/b/c.txt")
  println(b)
  val d = IO.dirname("/a/b/c.txt")
  println(d)
  val j = IO.join_path("a", "b")
  println(j)
  val e = IO.extension("file.iron")
  println(e)
}
EOF
./build/ironc build /tmp/test_io39.iron -o /tmp/test_io39 && /tmp/test_io39
```

Expected:
```
c.txt
/a/b
a/b
iron
```
</verification>

<success_criteria>
- io.iron contains 14 func declarations (6 existing + 8 new); read_bytes/write_bytes absent
- iron_io.h declares all 8 new C prototypes under `/* Phase 39 additions */`
- iron_io.c implements all 8 new functions appended after existing code
- io.extension strips the leading dot from the extension
- `cmake --build . -j4 --target ironc` exits 0 with no errors
</success_criteria>

<output>
After completion, create `/Users/victor/code/iron-lang/.planning/phases/39-module-completions-math-io-time-log/39-02-SUMMARY.md`
</output>
