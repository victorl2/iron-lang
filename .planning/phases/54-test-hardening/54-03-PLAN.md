---
phase: 54-test-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/compose_soa_fusion.iron
  - tests/integration/compose_soa_fusion.expected
  - tests/integration/compose_dead_field_compress.iron
  - tests/integration/compose_dead_field_compress.expected
  - tests/integration/compose_mono_fusion.iron
  - tests/integration/compose_mono_fusion.expected
  - tests/integration/compose_arena_soa_dead.iron
  - tests/integration/compose_arena_soa_dead.expected
  - tests/integration/compose_mega.iron
  - tests/integration/compose_mega.expected
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "SoA layout + fusion produces correct results (fused loop reads from per-field arrays)"
    - "Dead field elimination + value range compression coexist (compressed fields exclude dead ones)"
    - "Monomorphic collapse + fusion works (single-type fused loops with no split overhead)"
    - "Arena + SoA + dead field triple composition produces correct results"
    - "Mega test combining ALL optimizations produces correct results"
  artifacts:
    - path: "tests/integration/compose_soa_fusion.iron"
      provides: "SoA + fusion composition test"
    - path: "tests/integration/compose_soa_fusion.expected"
      provides: "Expected output for SoA + fusion test"
    - path: "tests/integration/compose_dead_field_compress.iron"
      provides: "Dead field + compression composition test"
    - path: "tests/integration/compose_dead_field_compress.expected"
      provides: "Expected output for dead field + compression test"
    - path: "tests/integration/compose_mono_fusion.iron"
      provides: "Monomorphic + fusion composition test"
    - path: "tests/integration/compose_mono_fusion.expected"
      provides: "Expected output for monomorphic + fusion test"
    - path: "tests/integration/compose_arena_soa_dead.iron"
      provides: "Arena + SoA + dead field triple composition test"
    - path: "tests/integration/compose_arena_soa_dead.expected"
      provides: "Expected output for arena + SoA + dead field test"
    - path: "tests/integration/compose_mega.iron"
      provides: "All-optimizations mega composition test"
    - path: "tests/integration/compose_mega.expected"
      provides: "Expected output for mega composition test"
  key_links:
    - from: "tests/integration/compose_soa_fusion.iron"
      to: "src/lir/layout_analysis.c"
      via: "SoA selection triggers when loop accesses <50% of fields; generated C has per-field arrays"
      pattern: "particle_x|bullet_x"
    - from: "tests/integration/compose_dead_field_compress.iron"
      to: "src/lir/value_range.c"
      via: "Range analysis compresses surviving fields after dead field elimination"
      pattern: "uint8_t"
    - from: "tests/integration/compose_mono_fusion.iron"
      to: "src/lir/emit_c.c"
      via: "Monomorphic detection + fusion chain detection coexist"
      pattern: "no SplitList.*fused"
    - from: "tests/integration/compose_mega.iron"
      to: "src/lir/emit_c.c"
      via: "All optimization passes fire on same program"
      pattern: "mega composition"
---

<objective>
Create composition tests that verify multiple compiler optimizations work correctly when combined. Tests four focused combinations (SoA+fusion, dead field+compression, monomorphic+fusion, arena+SoA+dead field) plus one mega test exercising ALL optimizations simultaneously.

Purpose: Individual optimizations are tested in isolation by existing tests. These composition tests catch interaction bugs where one optimization's output is another's input -- e.g., SoA layout changes memory layout, and fusion must read from the new per-field array structure.

Output: 5 test pairs (.iron + .expected) in tests/integration/ with compose_ prefix.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-test-hardening/54-CONTEXT.md

@tests/integration/layout_soa_select.iron
@tests/integration/layout_dead_field.iron
@tests/integration/value_range_compress.iron
@tests/integration/mono_single_type_collapse.iron
@tests/integration/fusion_split_map_filter_reduce.iron
@tests/integration/arena_split_collection.iron
@tests/integration/layout_annotation.iron
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SoA+fusion and dead field+compression composition tests</name>
  <files>
    tests/integration/compose_soa_fusion.iron,
    tests/integration/compose_soa_fusion.expected,
    tests/integration/compose_dead_field_compress.iron,
    tests/integration/compose_dead_field_compress.expected
  </files>
  <read_first>
    tests/integration/layout_soa_select.iron,
    tests/integration/layout_soa_select.expected,
    tests/integration/fusion_split_map_filter_reduce.iron,
    tests/integration/fusion_split_map_filter_reduce.expected,
    tests/integration/layout_dead_field.iron,
    tests/integration/layout_dead_field.expected,
    tests/integration/value_range_compress.iron,
    tests/integration/value_range_compress.expected
  </read_first>
  <action>
Create two composition test files.

**compose_soa_fusion.iron:**
Triggers BOTH SoA layout selection AND fusion chain on the same collection.
- Define Entity interface with func get_x() -> Int
- Define Particle(x, y, z, mass) and Bullet(x, y, z, speed) implementing Entity
  - Each has 4 fields; get_x() returns only self.x (1 of 4 fields = 25% < 50% threshold -> SoA)
- Create collection: val entities = [Particle(10,20,30,100), Bullet(5,15,25,50), Particle(3,6,9,200), Bullet(8,12,18,75)]
- Apply fusion chain that only accesses x: entities.map(func(e: Entity) -> Int { return e.get_x() }).filter(func(x: Int) -> Bool { return x > 4 }).sum()
- Expected: get_x values [10,5,3,8] -> filter >4: [10,5,8] -> sum = 23
- This forces the compiler to: (1) select SoA layout since only x is accessed, (2) fuse the map+filter+sum chain, (3) the fused loop must read from the per-field SoA x-array, not AoS structs
- Print the result

**compose_dead_field_compress.iron:**
Triggers BOTH dead field elimination AND value range compression on surviving fields.
- Define Particle interface with func describe() -> Int
- Define Dot(kind, x, y, debug_id) and Pixel(kind, r, g, debug_id) implementing Particle
  - kind holds values 1 or 2 (range 1-2 -> compressible to uint8_t)
  - x/y hold 10-50, r/g hold 0-255 (compressible to uint8_t)
  - debug_id is never accessed through the collection (dead field -> eliminated)
- describe() returns self.kind * 1000 + field1 * 100 + field2
- Create collection with 4-6 elements using small known values
- Use forEach to sum describe() results
- The compiler should: (1) eliminate debug_id from Stor structs, (2) compress kind/x/y/r/g to uint8_t in the storage struct
- Verify computed sum matches expected
- Print result

Create .expected files with hand-computed results.
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang && build/ironc build tests/integration/compose_soa_fusion.iron && diff <(build/ironc run tests/integration/compose_soa_fusion.iron) tests/integration/compose_soa_fusion.expected && grep -qE '[a-z]+_x\b' .iron-build/compose_soa_fusion.c && echo "PASS: SoA per-field array pattern in generated C" && build/ironc build tests/integration/compose_dead_field_compress.iron && diff <(build/ironc run tests/integration/compose_dead_field_compress.iron) tests/integration/compose_dead_field_compress.expected && grep -q 'uint8_t' .iron-build/compose_dead_field_compress.c && echo "PASS: uint8_t compression in generated C"</automated>
  </verify>
  <acceptance_criteria>
    - compose_soa_fusion.iron compiles and runs correctly
    - compose_soa_fusion output matches .expected (fused sum through SoA-selected collection)
    - Generated C for SoA+fusion contains per-field array naming pattern (e.g., particle_x, bullet_x) confirming SoA layout was applied
    - compose_dead_field_compress.iron compiles and runs correctly
    - compose_dead_field_compress output matches .expected (correct results with dead field eliminated AND surviving fields compressed)
    - Generated C for dead field+compress contains uint8_t (value range compression applied)
  </acceptance_criteria>
  <done>SoA+fusion composition test verifies fused loops work over SoA-layout collections; generated C confirms per-field array pattern. Dead field+compression test verifies compressed storage works after dead field elimination; generated C confirms uint8_t compression.</done>
</task>

<task type="auto">
  <name>Task 2: Create monomorphic+fusion, triple composition, and mega composition tests</name>
  <files>
    tests/integration/compose_mono_fusion.iron,
    tests/integration/compose_mono_fusion.expected,
    tests/integration/compose_arena_soa_dead.iron,
    tests/integration/compose_arena_soa_dead.expected,
    tests/integration/compose_mega.iron,
    tests/integration/compose_mega.expected
  </files>
  <read_first>
    tests/integration/mono_single_type_collapse.iron,
    tests/integration/mono_single_type_collapse.expected,
    tests/integration/fusion_flat_map_filter_reduce.iron,
    tests/integration/arena_split_collection.iron,
    tests/integration/arena_split_collection.expected,
    tests/integration/layout_annotation.iron
  </read_first>
  <action>
Create three composition test files.

**compose_mono_fusion.iron:**
Triggers monomorphic collapse (single concrete type) combined with fusion.
- Define Shape interface with func area() -> Int
- Define ONLY Circle implementing Shape (single implementor -> monomorphic collapse)
- Create val shapes = [Circle(5), Circle(3), Circle(7), Circle(2), Circle(10)]
  - Type inference sees all Circle -> should collapse to plain typed array (no SplitList)
- Chain .map(func(s: Shape) -> Int { return s.area() }).filter(func(a: Int) -> Bool { return a > 20 }).reduce(0, func(acc: Int, x: Int) -> Int { return acc + x })
  - Areas: [75, 27, 147, 12, 300]. Filter >20: [75, 27, 147, 300]. Sum = 549.
- The compiler must: (1) detect monomorphic collection (skip SplitList), (2) still apply fusion on the flat array path
- Print result

**compose_arena_soa_dead.iron:**
Triple composition: arena allocation + SoA layout + dead field elimination.
- Define Entity interface with func get_x() -> Int
- Define Particle(x, y, z, mass, debug) and Bullet(x, y, z, speed, debug) implementing Entity
  - 5 fields each; get_x() returns only self.x (1 of 5 = 20% < 50% -> SoA)
  - debug field never accessed through collection (dead field)
- Create a collection with >8 elements (triggers arena reallocation beyond initial capacity)
  - Use 12-16 elements mixing Particle and Bullet
- Iterate with forEach summing get_x() values
- The compiler should: (1) use arena allocation for the split sub-arrays, (2) select SoA layout, (3) eliminate debug field from Stor structs
- Print result

**compose_mega.iron:**
Mega test combining ALL optimizations per CONTEXT.md specifics:
- Define an interface with 3+ implementors (triggers split collection + arena + tag dispatch)
- Use `layout: soa` annotation on the collection (triggers SoA)
- Apply a fused map+filter+reduce chain (triggers fusion)
- Include dead fields on at least one implementor (triggers dead field elimination)
- Use small-range field values (triggers value range compression)
- Have >8 elements (triggers arena geometric growth)
- Example structure:
  - interface Widget with func score() -> Int
  - object Button(priority, x, debug_tag) impl Widget -- debug_tag = dead field
  - object Slider(priority, pos, debug_tag) impl Widget -- debug_tag = dead field
  - object Label(priority, len, debug_tag) impl Widget -- debug_tag = dead field
  - priority holds 1-5 (compressible), x/pos/len hold 1-100 (compressible)
  - score() uses priority and field2 only (not debug_tag)
  - Create 12+ elements with all 3 types
  - Apply: widgets.map(score).filter(>10).sum()
  - This exercises: SoA (partial field access), fusion (map+filter+sum), dead field (debug_tag), compression (small ranges), arena (>8 elements), split collection (3 types)
- Compute expected result carefully. Print it.

Create .expected files with hand-computed results. Double-check all arithmetic.
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang && build/ironc build tests/integration/compose_mono_fusion.iron && diff <(build/ironc run tests/integration/compose_mono_fusion.iron) tests/integration/compose_mono_fusion.expected && grep -qv 'Iron_SplitList_' .iron-build/compose_mono_fusion.c && echo "PASS: no SplitList in mono+fusion generated C" && build/ironc build tests/integration/compose_arena_soa_dead.iron && diff <(build/ironc run tests/integration/compose_arena_soa_dead.iron) tests/integration/compose_arena_soa_dead.expected && build/ironc build tests/integration/compose_mega.iron && diff <(build/ironc run tests/integration/compose_mega.iron) tests/integration/compose_mega.expected && grep -q 'uint8_t' .iron-build/compose_mega.c && echo "PASS: uint8_t in mega generated C" && grep -qE 'fused|fusion' .iron-build/compose_mega.c && echo "PASS: fused loop in mega generated C"</automated>
  </verify>
  <acceptance_criteria>
    - compose_mono_fusion.iron compiles and produces correct fused result on monomorphic collection
    - Generated C for mono+fusion does NOT contain Iron_SplitList_ (monomorphic collapse confirmed)
    - compose_arena_soa_dead.iron compiles and produces correct result with arena+SoA+dead field
    - compose_mega.iron compiles and produces correct result exercising ALL optimizations simultaneously
    - Generated C for mega contains uint8_t (compression) and fused loop indicator
    - All 3 outputs match their .expected files
    - compose_mega.iron has 3+ implementors, >8 elements, dead fields, small-range fields, and a fusion chain
  </acceptance_criteria>
  <done>Monomorphic+fusion, arena+SoA+dead field triple, and mega all-optimizations tests compile, run, and produce correct results. Generated C confirms monomorphic collapse (no SplitList), compression (uint8_t), and fusion (fused loop) patterns.</done>
</task>

</tasks>

<verification>
All 5 composition test pairs exist and pass:
```bash
cd /Users/victor/code/iron-lang
for f in tests/integration/compose_*.iron; do
  name=$(basename "$f" .iron)
  echo "Testing $name..."
  build/ironc build "$f"
  diff <(build/ironc run "$f") "tests/integration/${name}.expected" && echo "PASS: output" || echo "FAIL: output"
done

# Generated C checks (per CONTEXT.md locked decision: runtime output + generated C checks)
echo "--- Generated C pattern checks ---"
grep -qE '[a-z]+_x\b' .iron-build/compose_soa_fusion.c && echo "PASS: SoA per-field array in soa_fusion" || echo "FAIL"
grep -q 'uint8_t' .iron-build/compose_dead_field_compress.c && echo "PASS: uint8_t in dead_field_compress" || echo "FAIL"
grep -qv 'Iron_SplitList_' .iron-build/compose_mono_fusion.c && echo "PASS: no SplitList in mono_fusion" || echo "FAIL"
grep -q 'uint8_t' .iron-build/compose_mega.c && echo "PASS: uint8_t in mega" || echo "FAIL"
grep -qE 'fused|fusion' .iron-build/compose_mega.c && echo "PASS: fused in mega" || echo "FAIL"
```
</verification>

<success_criteria>
- 5 compose_* .iron files exist in tests/integration/
- 5 corresponding .expected files exist
- All 5 tests compile and produce correct output matching .expected
- SoA+fusion test accesses <50% of fields with a fusion chain; generated C has per-field array naming (e.g., particle_x)
- Dead field+compression test has unused fields AND small-range surviving fields; generated C has uint8_t
- Monomorphic+fusion test has single implementor with fusion chain; generated C has no SplitList
- Arena+SoA+dead field test has >8 elements with partial field access and dead fields
- Mega test combines 3+ implementors, SoA, fusion, dead fields, compression, arena (>8 elements); generated C confirms uint8_t and fused loop
</success_criteria>

<output>
After completion, create `.planning/phases/54-test-hardening/54-03-SUMMARY.md`
</output>
