---
phase: 66-structural-protections-linux-release-ci
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tests/integration/run_integration.sh
  - docs/regression-fixtures.md
autonomous: true
requirements: [REG-01, REG-04]
must_haves:
  truths:
    - "A new CI job 'build-and-test-release (ubuntu-latest)' exists in .github/workflows/ci.yml that builds ironc in Release mode and runs the integration test suite"
    - "The new Release job is a one-line-diff from the existing Debug job: CMAKE_BUILD_TYPE=Release and the 'Verify sanitizers' step omitted"
    - "tests/integration/run_integration.sh uses set -euo pipefail and propagates ironc crash exit codes explicitly with anti-pattern comments"
    - "docs/regression-fixtures.md exists and documents the 4-section template, explicitly citing tests/integration/hir_to_lir_elif_mono_walker.iron as the canonical example"
    - "A crash in any integration test in the new Release job fails the CI check (not swallowed as 'benchmark failed' noise)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "build-and-test-release job alongside existing build-and-test job"
      contains: "build-and-test-release:"
    - path: "tests/integration/run_integration.sh"
      provides: "Tightened exit-code propagation with anti-pattern comments"
      contains: "do not swallow SIGSEGV"
    - path: "docs/regression-fixtures.md"
      provides: "Contributor-facing template documentation with 4-section breakdown"
      contains: "hir_to_lir_elif_mono_walker.iron"
  key_links:
    - from: ".github/workflows/ci.yml build-and-test-release job"
      to: "tests/integration/run_integration.sh"
      via: "CI step invokes the script with build/ironc argument"
      pattern: "run_integration.sh"
    - from: "tests/integration/run_integration.sh ironc invocation"
      to: "explicit exit-code propagation"
      via: "|| { echo crashed; exit 1; }"
      pattern: "ironc crashed"
    - from: "docs/regression-fixtures.md"
      to: "tests/integration/hir_to_lir_elif_mono_walker.iron"
      via: "explicit markdown reference to the canonical example"
      pattern: "hir_to_lir_elif_mono_walker"
---

<objective>
Add the Linux Release CI job that the v0.1.4-alpha motivating incident exposed as missing, harden the integration test runner so ironc crashes can never again be swallowed as generic test failures, and land the contributor-facing regression-fixture template documentation that REG-04 requires.

Purpose: Satisfies REG-01 (new CI job) and REG-04 (template documentation). REG-01 is the missing CI signal that would have caught the `find_first_last` SIGSEGV months earlier — the Benchmark workflow's "benchmark compilation failed" status was hiding compile crashes. REG-04 formalizes the `hir_to_lir_elif_mono_walker.iron` 4-section doc-comment pattern as the canonical template for Phase 66 Plan 03's new PROT-04 fixtures AND for Phase 67's REG-02 crash canary fixtures AND for every future AUDIT-derived regression test.

Output: Modified .github/workflows/ci.yml (new job), modified tests/integration/run_integration.sh (exit-code hardening), new docs/regression-fixtures.md file. No C source changes. Fully independent of Plans 01-03 — can run in parallel with Plan 01.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-structural-protections-linux-release-ci/66-CONTEXT.md
@.github/workflows/ci.yml
@tests/integration/run_integration.sh
@tests/integration/hir_to_lir_elif_mono_walker.iron

<interfaces>
Current .github/workflows/ci.yml structure (the existing Debug job that the new Release job mirrors):

```yaml
name: CI

on:
  push:
    branches: [main]
    paths: [...]
  pull_request:
    branches: [main]
    paths: [...]

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build clang gdb
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install cmake ninja
      - name: Configure (Debug with ASan/UBSan)
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_C_COMPILER=clang \
            -G Ninja
      - name: Build
        run: cmake --build build
      - name: Run unit tests
        env:
          ASAN_OPTIONS: detect_leaks=0
        run: ctest --test-dir build --output-on-failure -j4 -E "benchmark"
      - name: Verify binaries exist
        run: |
          test -x build/iron && echo "iron binary OK"
          test -x build/ironc && echo "ironc binary OK"
      - name: Run integration tests
        env:
          ASAN_OPTIONS: detect_leaks=0
        run: |
          chmod +x tests/integration/run_integration.sh
          tests/integration/run_integration.sh build/ironc
      - name: Verify sanitizers
        env:
          ASAN_OPTIONS: detect_leaks=0:abort_on_error=1
          UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
        run: ctest --test-dir build --output-on-failure -j4 -E "benchmark"
```

Note: The existing Debug job passes `build/ironc` to run_integration.sh, but run_integration.sh expects the `iron` binary path (line 16: IRON_BIN_ARG defaults to build/iron). The existing step is technically mismatched — for the new Release job, the executor should pass `build/iron` to match the script's contract OR leave the existing ci.yml behavior (pass ironc) if investigation shows it works as-is. Verify with a local run before committing.

Current tests/integration/run_integration.sh (full file, ~95 lines):

```bash
#!/bin/bash
# run_integration.sh - Integration test runner for the Iron compiler.
# ... (header comment)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
IRON_BIN_ARG="${1:-${SCRIPT_DIR}/../../build/iron}"
IRON_BIN="$(cd "$(dirname "${IRON_BIN_ARG}")" && pwd)/$(basename "${IRON_BIN_ARG}")"
# ...

for test_file in "${SCRIPT_DIR}"/*.iron; do
    # ...
    if ! (cd "${build_dir}" && "${IRON_BIN}" build "${test_file}") 2>"${build_stderr}"; then
        echo "[FAIL] (build failed)"
        cat "${build_stderr}" >&2
        FAIL=$((FAIL + 1))
        continue
    fi
    # ...
done
```

The script already has `set -euo pipefail` at line 13. The current ironc invocation pattern uses `if ! (...)` which catches non-zero exits but does NOT distinguish between "clean compile error (exit 1)" and "ironc crashed (SIGSEGV, exit 139)". The hardening target: add explicit crash detection on exit codes >= 128 (signal indicators) and surface them separately so CI logs clearly show "ironc crashed" vs "ironc emitted compile error".

Current reference fixture (tests/integration/hir_to_lir_elif_mono_walker.iron — first 32 lines define the 4-section doc-comment pattern):

```
-- Regression: collect_mono_enums_node misreading Iron_IfStmt layout.
--
-- **Motivating Incident.** ...
-- **Layout Diagram.** ...
-- **Fix Summary.** ...
-- **Severity.** ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Harden tests/integration/run_integration.sh exit-code propagation with crash detection and anti-pattern comments</name>
  <files>tests/integration/run_integration.sh</files>
  <read_first>
    - tests/integration/run_integration.sh (full file — confirm current set -e behavior, ironc invocation pattern, and any existing exit-code handling)
    - .planning/phases/66-structural-protections-linux-release-ci/66-CONTEXT.md decisions REG-01 section (specifies the 4-point hardening checklist)
  </read_first>
  <behavior>
    - When ironc returns a signal-level exit code (>= 128), the script prints "ironc crashed" with the exit code and the fixture name to stderr and exits 1 immediately.
    - When ironc returns a normal compile-error exit code (e.g., 1), the script still reports [FAIL] (build failed) as before.
    - `set -euo pipefail` is in place (it already is — verify).
    - Every ironc invocation line has a comment explaining why SIGSEGV must not be swallowed (the motivating incident).
    - Running the script against the current tree still reports PASS/FAIL as before for all existing fixtures.
  </behavior>
  <action>
The script already has `set -euo pipefail` at line 13 (verified pre-edit). The hardening is in the loop body where ironc is invoked.

Current invocation block (approximately lines 56-63):

```bash
if ! (cd "${build_dir}" && "${IRON_BIN}" build "${test_file}") 2>"${build_stderr}"; then
    echo "[FAIL] (build failed)"
    cat "${build_stderr}" >&2
    FAIL=$((FAIL + 1))
    continue
fi
```

Replace with a variant that captures the exit code explicitly and distinguishes crash from clean failure. Because `set -e` is active, use a pattern that temporarily disables it around the ironc call:

```bash
# Run ironc on the fixture. CRITICAL anti-pattern prevention:
# do not swallow SIGSEGV (exit code 139) or other crash signals as
# generic "test failed" noise. The Phase 65/66 motivating incident was
# that the Benchmark workflow reported "find_first_last: Iron compilation
# failed" for a SIGSEGV in ironc, hiding the crash for months. Exit codes
# >= 128 indicate a signal; treat those as a hard CI failure that cannot
# be explained away as a per-fixture issue.
set +e
(cd "${build_dir}" && "${IRON_BIN}" build "${test_file}") 2>"${build_stderr}"
ironc_exit=$?
set -e

if [ "${ironc_exit}" -ge 128 ]; then
    # Signal-level exit: ironc crashed (SIGSEGV=139, SIGABRT=134, etc.).
    # Do not count this as a per-fixture failure — abort the whole script.
    echo ""
    echo "[CRASH] ironc crashed on fixture ${test_name} (exit=${ironc_exit})" >&2
    echo "        Signal-level exit code — ironc hit a SIGSEGV or SIGABRT." >&2
    echo "        This is NOT a test failure; it is a compiler crash that" >&2
    echo "        the pre-Phase 66 CI would have swallowed as 'build failed'." >&2
    echo "        Stderr from ironc follows:" >&2
    cat "${build_stderr}" >&2
    exit 1
fi

if [ "${ironc_exit}" -ne 0 ]; then
    # Normal non-zero exit: ironc emitted a compile error and terminated cleanly.
    echo "[FAIL] (build failed, exit=${ironc_exit})"
    cat "${build_stderr}" >&2
    FAIL=$((FAIL + 1))
    continue
fi
```

Preserve all surrounding code (PASS/FAIL counters, expected-file comparison, temp directory cleanup). Do NOT change the script's CLI surface (argument parsing, IRON_BIN resolution). Do NOT remove the existing `set -euo pipefail` at line 13.

Add a second anti-pattern comment near the top of the script (just after the shebang + header comment) summarizing the crash-swallow policy so future contributors reading the file see the rationale immediately:

```bash
# Exit-code policy (Phase 66 REG-01):
# - Exit 0: all tests passed.
# - Exit 1 via FAIL counter: one or more tests failed but no crashes.
# - Exit 1 via "ironc crashed": a signal-level exit from ironc (>= 128)
#   is reported distinctly and fails the script immediately without
#   incrementing the FAIL counter. This prevents the Phase 65/66 motivating
#   incident where a SIGSEGV in ironc was reported as "benchmark compilation
#   failed" and hid for months. Do not swallow SIGSEGV as test-failed noise.
```

Run the script locally against the existing tree (`tests/integration/run_integration.sh build/iron`) to confirm every existing fixture still reports the same PASS/FAIL status. If the script previously passed build/ironc to itself in ci.yml's step (see interfaces block), verify that the edit handles both argument forms identically — IRON_BIN resolution logic is unchanged.
  </action>
  <verify>
    <automated>grep -q "set -euo pipefail" tests/integration/run_integration.sh && grep -q "ironc crashed" tests/integration/run_integration.sh && grep -q "do not swallow SIGSEGV" tests/integration/run_integration.sh && grep -q "ironc_exit" tests/integration/run_integration.sh && grep -q "ge 128" tests/integration/run_integration.sh && bash -n tests/integration/run_integration.sh && tests/integration/run_integration.sh build/iron</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "set -euo pipefail" tests/integration/run_integration.sh` returns at least 1
    - `grep -c "ironc crashed" tests/integration/run_integration.sh` returns at least 2 (the crash-detection branch plus an anti-pattern comment)
    - `grep -c "do not swallow SIGSEGV" tests/integration/run_integration.sh` returns at least 1
    - `grep -c "ironc_exit" tests/integration/run_integration.sh` returns at least 2 (assignment and conditional check)
    - `grep -q "ironc_exit.*-ge.*128" tests/integration/run_integration.sh` succeeds (the signal-level exit detection)
    - `grep -q "find_first_last" tests/integration/run_integration.sh` succeeds (motivating-incident reference in comment)
    - `bash -n tests/integration/run_integration.sh` exits 0 (shell-syntax valid)
    - `tests/integration/run_integration.sh build/iron` exits 0 and reports "0 failed" on the pre-Plan 03 tree (i.e., no new fixtures yet introduced by Plan 03 break the runner)
  </acceptance_criteria>
  <done>run_integration.sh distinguishes crash exits from compile failures, prints distinct [CRASH] output, and contains two grep-visible anti-pattern comments explaining why SIGSEGV must not be swallowed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add build-and-test-release (ubuntu-latest) job to .github/workflows/ci.yml as a one-line-diff mirror of the Debug job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file — to locate the existing build-and-test job and understand its steps)
    - .github/workflows/web.yml (the existing parallel WebAssembly workflow, as a reference for "new CI job in the same repo" idiom)
    - .planning/phases/66-structural-protections-linux-release-ci/66-CONTEXT.md decisions REG-01 section (specifies job name verbatim, clang compiler, integration + unit tests, no sanitizer step, Release build type)
  </read_first>
  <behavior>
    - After the edit, `.github/workflows/ci.yml` has TWO top-level jobs under `jobs:` — the existing `build-and-test` (Debug/ASan) and the new `build-and-test-release`.
    - The new job runs on `ubuntu-latest` only (not a matrix — per CONTEXT decision).
    - The new job's steps are a near-copy of the Debug job's steps with: (a) CMAKE_BUILD_TYPE=Release, (b) the "Verify sanitizers" step removed, (c) the ASAN_OPTIONS env vars removed from the ctest and integration-test steps (Release has no sanitizers).
    - The new job uses clang (same as Debug — matches CONTEXT decision).
    - The new job runs both unit tests (ctest -E "benchmark") and integration tests (run_integration.sh).
    - A workflow-level syntax validation passes (yamllint or actionlint).
  </behavior>
  <action>
Append a new job after the existing `build-and-test` job in `.github/workflows/ci.yml`. Do not modify the existing job. Paste the following block at the end of the `jobs:` section (after the last step of the existing job):

```yaml
  build-and-test-release:
    # REG-01 (Phase 66): Linux Release CI job — the missing signal that would
    # have caught the Phase 65/66 motivating SIGSEGV months earlier. The
    # existing build-and-test job runs Debug with ASan/UBSan and catches
    # memory bugs; this job runs Release (no sanitizer instrumentation) and
    # catches crashes that only reproduce under optimized builds on Linux
    # x86_64 — exactly the class of bug that hit find_first_last.
    #
    # This job is intentionally a near-duplicate of the Debug job. If the
    # Debug job changes, this job should change the same way (same compiler,
    # same dep install, same test runner invocation). The only differences:
    #   1. CMAKE_BUILD_TYPE=Release instead of Debug
    #   2. No sanitizer env vars (Release has no ASan/UBSan instrumentation)
    #   3. "Verify sanitizers" step omitted
    # Compiler is clang (matches Debug job) so a Release-only failure is
    # unambiguously a build-type issue, not a compiler-difference issue.
    # gcc matrix deferred to a future CI-expansion phase.
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies (Linux)
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build clang gdb

      - name: Configure (Release, no sanitizers)
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER=clang \
            -G Ninja

      - name: Build
        run: cmake --build build

      - name: Run unit tests
        run: ctest --test-dir build --output-on-failure -j4 -E "benchmark"

      - name: Verify binaries exist
        run: |
          test -x build/iron && echo "iron binary OK"
          test -x build/ironc && echo "ironc binary OK"

      - name: Run integration tests
        run: |
          chmod +x tests/integration/run_integration.sh
          tests/integration/run_integration.sh build/iron
```

Notes:
- Do NOT add this job to the existing matrix (it is NOT a new OS — it's a new build type; keeping it as a separate job preserves the Debug job's matrix simplicity per CONTEXT).
- Do NOT add sanitizer env vars (ASAN_OPTIONS, UBSAN_OPTIONS) — Release builds don't instrument them, and adding them would be misleading.
- Do NOT remove the existing "Verify sanitizers" step from the Debug job — that step is still needed on Debug.
- Use `build/iron` as the argument to run_integration.sh (matches the script's declared contract at line 16: `IRON_BIN_ARG="${1:-${SCRIPT_DIR}/../../build/iron}"`). If the existing Debug job passes `build/ironc` and it works, keep it as-is in the existing job; use `build/iron` in the new Release job since the script specifically expects the `iron` wrapper that invokes ironc internally.
- The Release job does NOT depend on the Debug job — both run in parallel on push/PR, satisfying CONTEXT's "Release job is an additional required status check" design.
- YAML indentation matches the existing file (4 spaces). Verify with `bash -c "python3 -c 'import yaml; yaml.safe_load(open(\".github/workflows/ci.yml\"))'"` if available locally, or by pushing to a throwaway branch and letting GitHub Actions parse it.
  </action>
  <verify>
    <automated>grep -q "build-and-test-release:" .github/workflows/ci.yml && grep -q "CMAKE_BUILD_TYPE=Release" .github/workflows/ci.yml && grep -q "ubuntu-latest" .github/workflows/ci.yml && grep -q "REG-01" .github/workflows/ci.yml && ! grep -A 30 "build-and-test-release:" .github/workflows/ci.yml | grep -q "Verify sanitizers" && ! grep -A 30 "build-and-test-release:" .github/workflows/ci.yml | grep -q "ASAN_OPTIONS" && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  build-and-test-release:" .github/workflows/ci.yml` returns exactly 1
    - `grep -c "^  build-and-test:" .github/workflows/ci.yml` returns exactly 1 (the existing Debug job is NOT deleted or renamed)
    - `grep -c "CMAKE_BUILD_TYPE=Release" .github/workflows/ci.yml` returns at least 1
    - `grep -c "CMAKE_BUILD_TYPE=Debug" .github/workflows/ci.yml` returns at least 1 (existing job preserved)
    - `grep -c "REG-01" .github/workflows/ci.yml` returns at least 1 (comment referencing the requirement)
    - `grep -c "find_first_last" .github/workflows/ci.yml` returns at least 1 (motivating incident reference in comment)
    - `sed -n '/build-and-test-release:/,/^  [a-z]/p' .github/workflows/ci.yml | grep -c "Verify sanitizers"` returns 0 (sanitizer step NOT present in Release job)
    - `sed -n '/build-and-test-release:/,/^  [a-z]/p' .github/workflows/ci.yml | grep -c "ASAN_OPTIONS"` returns 0 (no sanitizer env vars in Release job)
    - `python3 -c "import yaml; data = yaml.safe_load(open('.github/workflows/ci.yml')); print(sorted(data['jobs'].keys()))"` outputs `['build-and-test', 'build-and-test-release']`
    - `sed -n '/build-and-test-release:/,/^  [a-z]/p' .github/workflows/ci.yml | grep -c "runs-on: ubuntu-latest"` returns 1
    - `sed -n '/build-and-test-release:/,/^  [a-z]/p' .github/workflows/ci.yml | grep -c "clang"` returns at least 1
    - The YAML parses cleanly via python3 + pyyaml (the command in &lt;verify&gt;)
  </acceptance_criteria>
  <done>.github/workflows/ci.yml now has two jobs: the existing build-and-test (Debug/ASan on ubuntu-latest + macos-latest) and the new build-and-test-release (Release, clang, ubuntu-latest, no sanitizer step). The YAML parses cleanly and matches the REG-01 spec verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create docs/regression-fixtures.md documenting the 4-section template and explicitly citing hir_to_lir_elif_mono_walker.iron as the canonical example</name>
  <files>docs/regression-fixtures.md</files>
  <read_first>
    - tests/integration/hir_to_lir_elif_mono_walker.iron (full file — the canonical template example; the doc must quote its doc-comment structure)
    - .planning/phases/66-structural-protections-linux-release-ci/66-CONTEXT.md decisions REG-04 section
    - .planning/REQUIREMENTS.md REG-04 text
    - ls /Users/victor/code/worker-1/iron-lang/docs/ (confirm docs/ directory exists or create it)
  </read_first>
  <behavior>
    - docs/regression-fixtures.md exists as a new file.
    - The document explicitly cites tests/integration/hir_to_lir_elif_mono_walker.iron as the canonical reference.
    - The document has four clearly-labeled sections matching the template: Motivating Incident, Layout Diagram, Fix Summary, Severity.
    - The document explains WHY each section exists (contributor-facing rationale).
    - The document lists every PROT-04 fixture from Plan 03 as initial adopters of the template (forward-reference — these fixtures land in Plan 03 after this file is written, but listing them here documents the expected shape).
    - The document references Phase 67 REG-02 crash-canary fixtures as the next adopter.
    - The document is committed and tracked by git.
  </behavior>
  <action>
Create docs/regression-fixtures.md. If the docs/ directory does not exist, create it first (`mkdir -p docs`). Write the following content verbatim (substitute only the actual fixture names if Plan 03 uses different ones):

```markdown
# Regression Fixture Template

Every regression fixture in `tests/integration/` that protects against a
specific class of bug (blind cast, enum switch incompleteness, layout
assumption, use-after-free, etc.) follows a 4-section doc-comment template.
This document formalizes the template and lists the fixtures that adopt it.

## Canonical Example

The reference fixture is
[`tests/integration/hir_to_lir_elif_mono_walker.iron`](../tests/integration/hir_to_lir_elif_mono_walker.iron).
Read it first before adding a new regression fixture. Its top-of-file
doc-comment establishes the pattern and explains every section with a real
motivating bug: the Phase 37 `collect_mono_enums_node` SIGSEGV that hid in
the Iron compiler for months because the Linux Release CI path reported
compile crashes as "benchmark compilation failed".

## The 4-Section Template

Every regression fixture begins with an Iron-comment doc block at the top
of the file. The block has exactly four labeled sections in this order:

### 1. Motivating Incident

**What happened, where, and when.** A short paragraph identifying:

- The phase / commit / date the bug was introduced.
- The file and function where the bug lived.
- The specific wrong assumption the code made (e.g., "local typedef
  guessed at the layout of `Iron_IfStmt` and was missing the `elif_conds[]`
  and `elif_bodies[]` fields").
- The user-visible symptom (silent wrong output, SIGSEGV on Linux
  x86_64 but not macOS arm64, wrong codegen, etc.).
- How the bug was discovered (CI report, grep, audit row, user bug report).

This section is the "story" — when a contributor sees a red test in the
future, they should be able to read this paragraph and immediately
understand which bug class they are staring at.

### 2. Layout Diagram

**The structural picture of the bug.** An ASCII diagram or short
code block showing:

- The memory layout of the structs or data involved.
- What the buggy code was reading or writing at which offset.
- How the real layout differs from the assumed layout.

For blind-cast regressions, this is typically a side-by-side of the
"assumed struct" vs. the "real struct" with the diverging field
highlighted. For enum-switch regressions, this might be a list of
enum values the switch handles vs. the ones it misses. For layout
assumption regressions, this is a sizeof / offsetof table.

### 3. Fix Summary

**Which file:line was changed and how.** A short paragraph identifying:

- The file(s) and line number(s) of the fix.
- The high-level change (e.g., "added `node->kind` guard before cast",
  "converted in-place reinterpret to fresh allocation", "added missing
  `case IRON_NODE_FOR:` to the walker switch").
- Which `PROT-0x` / `FIX-0x` / `REG-0x` requirement the fix covers.
- Cross-reference to the phase and plan that landed the fix.

This section makes the fix grep-able — a future contributor searching
for "where did we fix the Phase 37 SIGSEGV?" can grep the integration
directory for `Fix Summary` and find this entry.

### 4. Severity

**H / M / L from CORRECTNESS-AUDIT.md.** A short justification of the
blast radius:

- **H (High)** — crash, UB, data corruption, or security risk in the
  default execution path. Every user program hits this bug.
- **M (Medium)** — correctness risk under abnormal conditions (OOM,
  unusual input shapes, specific compiler optimization modes).
- **L (Low)** — theoretical or cosmetic. No known user-visible failure.

The severity rating must match the row's severity in
`.planning/research/CORRECTNESS-AUDIT.md`. If the audit does not list
the bug, cite the phase that discovered it instead.

## Template Skeleton

Copy this skeleton when adding a new regression fixture. Replace the
placeholder text and the Iron source below:

```
-- Regression: <short title of the bug class>.
--
-- **Motivating Incident.** <what phase/commit introduced the bug, what
-- function was wrong, what the wrong assumption was, what the user-visible
-- symptom was, how the bug was discovered>
--
-- **Layout Diagram.** <ASCII diagram or short description showing the
-- wrong vs. correct memory layout, or the missing enum cases, or the
-- sizeof/offsetof mismatch>
--
-- **Fix Summary.** <which file:line was changed, what the high-level
-- change was, which PROT-0x / FIX-0x / REG-0x requirement this covers,
-- which phase and plan landed the fix>
--
-- **Severity.** <H / M / L from CORRECTNESS-AUDIT.md with a one-sentence
-- blast-radius justification>

<Iron source code that exercises the bug>
```

## Fixtures Adopting This Template

The following fixtures follow the 4-section template. This list grows as
each phase adds regression tests derived from audit rows or real-world bug
reports.

### Reference

- `tests/integration/hir_to_lir_elif_mono_walker.iron` — The canonical
  example. Protects against a future regression in
  `collect_mono_enums_node`'s AST walker switch coverage.

### Phase 66 Plan 03 (PROT-04 blind-cast rewrites)

Landed alongside the typecheck.c / resolve.c / escape.c rewrites.

- `tests/integration/blind_cast_type_sym_decl.iron` — SYM_TYPE-&gt;decl_node
  cast to Iron_ObjectDecl without kind check (typecheck.c:1366, :1791).
- `tests/integration/blind_cast_expr_resolved_type.iron` — Iron_IntLit
  aliasing to access resolved_type on a generic expression node
  (typecheck.c:2707, :3070).
- `tests/integration/blind_cast_owner_decl.iron` — owner_sym-&gt;decl_node
  cast in super handler without kind check (resolve.c:236, :251).
- `tests/integration/enum_construct_reinterpret.iron` — in-place
  reinterpretation of Iron_EnumConstruct as Iron_MethodCallExpr /
  Iron_FieldAccess (resolve.c:674, :680).
- `tests/integration/blind_cast_expr_common_layout.iron` — runtime
  safety-net for the Iron_ExprNode common prefix assumption
  (hir_lower.c:109; primary defense is PROT-01's _Static_assert in
  src/parser/ast.h).
- `tests/integration/blind_cast_leak_ident.iron` — Iron_Ident cast in
  leak statement without kind check (escape.c:251).

### Phase 67 REG-02 (crash-canary fixtures — planned)

Phase 67 will add one fixture per AST node kind that the HIR-to-LIR walker
handles. Each follows this template with the Motivating Incident section
quoting the specific walker incompleteness the canary targets.

## Why This Matters

The v0.1.4-alpha Compiler Correctness milestone exists because a single
SIGSEGV in `collect_mono_enums_node` lived in the tree for months. The
Phase 59 Benchmark workflow caught it intermittently but reported it as
"benchmark compilation failed" — a generic failure class that hid the
signal. The response is threefold:

1. **Prevent.** PROT-01..04 structural protections make the bug class
   impossible to reintroduce silently.
2. **Detect.** REG-01 runs the Linux Release CI job that would have
   caught the original crash, and `tests/integration/run_integration.sh`
   now refuses to swallow SIGSEGV as generic test failure noise.
3. **Document.** Every fix lands with a regression fixture that cites the
   motivating incident. Future contributors reading a red test know
   exactly which bug class they are staring at — and why the fix looks
   the way it does.

This template is the third leg. Do not skip it.

## See Also

- `.planning/research/CORRECTNESS-AUDIT.md` — The ranked audit findings
  every PROT-04 / FIX-01..04 regression fixture cites.
- `.planning/REQUIREMENTS.md` — Requirements PROT-01..04, REG-01, REG-04
  for v0.1.4-alpha.
- `.github/workflows/ci.yml` — The `build-and-test-release` CI job that
  runs these fixtures in Release mode to catch optimization-dependent
  regressions.
```

Verify the document renders cleanly in any Markdown viewer and that all
internal links resolve. Commit the file via `git add docs/regression-fixtures.md`.

If Plan 03 Tasks 1-3 use different fixture names than listed in the "Phase 66
Plan 03" section above, update this file in the same commit (or in a
follow-up Plan 04 edit) to match — the fixture names here must stay in
sync with what Plan 03 actually lands.
  </action>
  <verify>
    <automated>test -f docs/regression-fixtures.md && grep -q "hir_to_lir_elif_mono_walker.iron" docs/regression-fixtures.md && grep -q "Motivating Incident" docs/regression-fixtures.md && grep -q "Layout Diagram" docs/regression-fixtures.md && grep -q "Fix Summary" docs/regression-fixtures.md && grep -q "Severity" docs/regression-fixtures.md && grep -q "blind_cast_type_sym_decl" docs/regression-fixtures.md && grep -q "REG-01" docs/regression-fixtures.md && grep -q "REG-04" docs/regression-fixtures.md && grep -q "PROT-04" docs/regression-fixtures.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/regression-fixtures.md` exists
    - `grep -c "hir_to_lir_elif_mono_walker.iron" docs/regression-fixtures.md` returns at least 2 (canonical example reference + the reference fixture entry)
    - `grep -c "Motivating Incident" docs/regression-fixtures.md` returns at least 2 (section heading + template skeleton)
    - `grep -c "Layout Diagram" docs/regression-fixtures.md` returns at least 2
    - `grep -c "Fix Summary" docs/regression-fixtures.md` returns at least 2
    - `grep -c "Severity" docs/regression-fixtures.md` returns at least 2
    - `grep -c "PROT-01\\|PROT-02\\|PROT-03\\|PROT-04\\|REG-01\\|REG-04" docs/regression-fixtures.md` returns at least 5 (at least 5 requirement IDs referenced)
    - `grep -c "blind_cast_type_sym_decl\\|blind_cast_expr_resolved_type\\|blind_cast_owner_decl\\|enum_construct_reinterpret\\|blind_cast_expr_common_layout\\|blind_cast_leak_ident" docs/regression-fixtures.md` returns at least 6 (all 6 PROT-04 fixture names listed)
    - `grep -c "find_first_last\\|collect_mono_enums_node" docs/regression-fixtures.md` returns at least 1 (motivating incident cited)
    - `grep -c "CORRECTNESS-AUDIT.md" docs/regression-fixtures.md` returns at least 1
    - File word count >= 600 (non-trivial content, not a stub): `wc -w docs/regression-fixtures.md | awk '{print $1}'` is at least 600
  </acceptance_criteria>
  <done>docs/regression-fixtures.md exists, documents the 4-section template with contributor-facing rationale, cites hir_to_lir_elif_mono_walker.iron as the canonical example, lists all 6 Plan 03 PROT-04 fixtures as initial adopters, and forward-references Phase 67 REG-02 crash canaries as the next adopter.</done>
</task>

</tasks>

<verification>
Phase-level checks for Plan 04:
- `.github/workflows/ci.yml` has two top-level jobs: build-and-test (unchanged Debug) and build-and-test-release (new Release/ubuntu-latest/clang).
- The new job builds in Release mode, runs unit tests, runs integration tests, and skips the sanitizer step.
- `tests/integration/run_integration.sh` distinguishes crash exits (>= 128) from compile failures and prints a distinct [CRASH] report that fails the script immediately.
- The script has `set -euo pipefail` AND explicit anti-pattern comments citing the Phase 65/66 motivating incident.
- `docs/regression-fixtures.md` exists, documents the 4-section template, and cites the canonical reference fixture + all 6 Plan 03 adopter fixtures.
- `bash -n tests/integration/run_integration.sh` exits 0.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
- The new REG-01 job would catch any ironc SIGSEGV triggered by a Plan 03 regression fixture (verifiable by running locally with CMAKE_BUILD_TYPE=Release and deliberately breaking a fix).
</verification>

<success_criteria>
- Linux Release CI job exists in ci.yml and runs alongside the existing Debug job.
- Integration test runner cannot swallow ironc crashes — signal-level exits produce distinct [CRASH] output.
- Anti-pattern comments citing the motivating incident exist in run_integration.sh at grep-discoverable locations.
- docs/regression-fixtures.md documents the 4-section template with contributor-facing rationale, canonical example, and adopter fixture list.
- No C source changes; no test regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/66-structural-protections-linux-release-ci/66-04-SUMMARY.md` documenting:
- The exact .github/workflows/ci.yml diff (job added, no existing job modified)
- The run_integration.sh hardening diff
- Word count and section list of docs/regression-fixtures.md
- Local verification that the Release CI job would run (even if GitHub Actions cannot be tested offline)
- Any pitfalls (YAML syntax quirks, bash set -e interaction with subshells, Markdown link resolution)
</output>
