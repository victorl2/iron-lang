---
phase: 52-emitter-refactoring
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - src/lir/emit_structs.h
  - src/lir/emit_structs.c
  - src/lir/emit_c.c
  - CMakeLists.txt
autonomous: true
requirements: [EMIT-03]

must_haves:
  truths:
    - "Object struct body emission (topo sort, struct fields, type tags) lives in emit_structs.c"
    - "Tagged union generation (tag enums, union types, wrapping constructors) lives in emit_structs.c"
    - "emit_type_decls is callable from emit_c.c via emit_structs.h API"
    - "emit_c.c no longer contains struct/type declaration generation code"
    - "All existing tests pass identically"
  artifacts:
    - path: "src/lir/emit_structs.h"
      provides: "Public API for struct and type declaration emission"
      contains: "emit_type_decls"
    - path: "src/lir/emit_structs.c"
      provides: "Implementation of struct emission, topo sort, tagged unions"
      contains: "emit_object_struct_body"
    - path: "CMakeLists.txt"
      provides: "Build system includes emit_structs.c"
      contains: "emit_structs.c"
  key_links:
    - from: "src/lir/emit_c.c"
      to: "src/lir/emit_structs.h"
      via: "#include"
      pattern: '#include "lir/emit_structs.h"'
    - from: "src/lir/emit_structs.c"
      to: "src/lir/emit_helpers.h"
      via: "#include"
      pattern: '#include "lir/emit_helpers.h"'
---

<objective>
Extract struct/layout emission into emit_structs.c/h — object struct bodies, topological sorting, tagged union generation (tag enums, data unions, wrapping constructors), and the emit_type_decls orchestrator function.

Purpose: Separates type declaration emission (~1100 lines) from the core instruction emission. After this, emit_c.c calls emit_type_decls() as a black box and the struct generation logic is independently readable.

Output: New emit_structs.c/h files, slimmer emit_c.c.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-emitter-refactoring/52-CONTEXT.md
@.planning/phases/52-emitter-refactoring/52-01-SUMMARY.md

<interfaces>
<!-- From emit_helpers.h (created in Plan 01) — executor will have these available -->

Key functions used by emit_structs:
```c
const char *emit_mangle_name(const char *name, Iron_Arena *arena);
const char *emit_type_to_c(const Iron_Type *t, EmitCtx *ctx);
const char *emit_annotation_to_c(const char *name, EmitCtx *ctx);
void emit_ensure_optional(EmitCtx *ctx, const Iron_Type *inner);
```

Key types from emit_helpers.h:
```c
typedef struct { ... } EmitCtx;  // Full struct definition
```

From src/lir/emit_c.c — code sections to extract:

Lines ~5534-5546: IrTopoState typedef and topo constants
Lines ~5548-5592: find_ir_type_decl_idx, ir_topo_visit  
Lines ~5595-5607: ir_has_subtype
Lines ~5629-5698: emit_object_struct_body
Lines ~5700-5718: estimate_type_size
Lines ~5778-6661: emit_type_decls (the large function)
  - Forward declarations (5782-5793)
  - Topo sort (5795-5827)
  - Interface tagged union structs (5829-5970)
  - Split collection struct generation (5971-6489) — NOTE: this section will be extracted to emit_split.c in Plan 03, but for this plan it stays in emit_structs.c temporarily
  - Interface dispatch functions (6490-6661) — NOTE: this includes the interface dispatch function generation starting around line 6490

The split collection code inside emit_type_decls (lines 5971-6489) is tightly coupled to the rest of emit_type_decls. For this plan, move ALL of emit_type_decls as a single unit to emit_structs.c. Plan 03 will then extract the split collection portion into emit_split.c.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create emit_structs.h/c with type declaration emission</name>
  <files>src/lir/emit_structs.h, src/lir/emit_structs.c</files>
  <read_first>src/lir/emit_c.c, src/lir/emit_helpers.h</read_first>
  <action>
**A. Create src/lir/emit_structs.h:**

1. Header guard: IRON_EMIT_STRUCTS_H
2. Top-of-file comment: "emit_structs.h -- Struct body, tagged union, and type declaration emission."
3. Include "lir/emit_helpers.h" (provides EmitCtx and shared helpers)
4. Declare public API:
   ```c
   /* Emit all type declarations: forward decls, struct bodies (topo-sorted),
    * tagged union types, wrapping constructors, split collection structs,
    * push/free functions, and interface dispatch functions.
    * Writes to ctx->forward_decls, ctx->struct_bodies, ctx->prototypes,
    * and ctx->lifted_funcs. */
   void emit_type_decls(EmitCtx *ctx);

   /* Estimate size of a concrete type in bytes (for variant split decisions). */
   int emit_estimate_type_size(Iron_ObjectDecl *od);
   ```

**B. Create src/lir/emit_structs.c:**

1. Include emit_structs.h, plus system headers and vendor/stb_ds.h, parser/ast.h.
2. Move these items from emit_c.c, making them either static (internal) or non-static (declared in header):

   Internal (keep static in emit_structs.c):
   - IR_TOPO_WHITE/GRAY/BLACK defines (line 5537-5539)
   - IrTopoState typedef (lines 5541-5546)
   - find_ir_type_decl_idx (line 5549) — static
   - ir_topo_visit (line 5559) — static
   - ir_has_subtype (line 5595) — static
   - emit_object_struct_body (line 5629) — static

   Public (declared in header, non-static):
   - estimate_type_size (line 5702) -> emit_estimate_type_size (renamed with prefix)
   - emit_type_decls (line 5778) — keep name, remove static

3. Inside emit_type_decls and emit_object_struct_body, update all calls to use the new names from emit_helpers.h:
   - `emit_mangle_name(...)` — same name (was already emit_ prefixed)
   - `annotation_to_c(...)` -> `emit_annotation_to_c(...)`
   - `estimate_type_size(...)` -> `emit_estimate_type_size(...)`

4. The large emit_type_decls function includes split collection struct generation, push functions, free functions, and interface dispatch generation. Move ALL of this as a single unit for now. Plan 03 will extract the split-specific portions.
  </action>
  <verify>
    <automated>test -f src/lir/emit_structs.h && test -f src/lir/emit_structs.c && grep -q "emit_type_decls" src/lir/emit_structs.h && grep -q "emit_object_struct_body" src/lir/emit_structs.c && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- emit_structs.h declares emit_type_decls and emit_estimate_type_size
- emit_structs.c contains IrTopoState, ir_topo_visit, emit_object_struct_body, emit_type_decls
- emit_structs.c includes emit_helpers.h
- No compilation errors in emit_structs.c standalone
  </acceptance_criteria>
  <done>emit_structs.h/c exist with all type declaration emission code extracted</done>
</task>

<task type="auto">
  <name>Task 2: Update emit_c.c and CMakeLists.txt, verify all tests</name>
  <files>src/lir/emit_c.c, CMakeLists.txt</files>
  <read_first>src/lir/emit_c.c, src/lir/emit_structs.h, CMakeLists.txt</read_first>
  <action>
**A. Update src/lir/emit_c.c:**

1. Add `#include "lir/emit_structs.h"` after the emit_helpers.h include.

2. Remove from emit_c.c all code that was moved to emit_structs.c:
   - The IR_TOPO defines (lines ~5537-5539)
   - The IrTopoState typedef (lines ~5541-5546)
   - find_ir_type_decl_idx function (lines ~5548-5557)
   - ir_topo_visit function (lines ~5559-5592)
   - ir_has_subtype function (lines ~5595-5607)
   - emit_object_struct_body function (lines ~5629-5698)
   - estimate_type_size function (lines ~5700-5718)
   - emit_type_decls function (lines ~5778-6661) — this is the big one (~884 lines)

3. In iron_lir_emit_c, the call to `emit_type_decls(&ctx)` at line ~6866 stays as-is (same function name, now resolved through emit_structs.h).

4. Update any remaining call to `estimate_type_size` (if any exist outside the moved code) to `emit_estimate_type_size`.

**B. Update CMakeLists.txt:**

Add `src/lir/emit_structs.c` to the iron_compiler STATIC library, right after `src/lir/emit_helpers.c`.

**C. Build and test:**

Run cmake configure + build + full test suite. All tests must pass.
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang && cmake --build build 2>&1 | tail -5 && cd build && ctest --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- emit_c.c no longer contains emit_type_decls function body (grep -c "^static void emit_type_decls" src/lir/emit_c.c returns 0)
- emit_c.c no longer contains IrTopoState (grep -c "IrTopoState" src/lir/emit_c.c returns 0)
- emit_c.c no longer contains emit_object_struct_body (grep -c "emit_object_struct_body" src/lir/emit_c.c returns 0)
- emit_c.c includes emit_structs.h
- CMakeLists.txt lists emit_structs.c
- Full build succeeds with zero warnings from emit_structs.c
- All tests pass (ctest --output-on-failure)
  </acceptance_criteria>
  <done>emit_c.c reduced by ~1100 lines of struct emission; emit_structs.c handles all type declaration emission; all tests pass</done>
</task>

</tasks>

<verification>
1. `wc -l src/lir/emit_structs.c` shows roughly 1000-1200 lines
2. `wc -l src/lir/emit_c.c` shows roughly 5900-6100 lines (down from 7120)
3. `grep -c "emit_type_decls\|emit_object_struct_body\|IrTopoState\|ir_topo_visit\|ir_has_subtype\|estimate_type_size" src/lir/emit_c.c` returns 0 or near-0 (only call sites, no definitions)
4. `cd build && ctest --output-on-failure` — all tests pass
</verification>

<success_criteria>
- All struct/type emission code lives in emit_structs.c/h
- emit_c.c calls emit_type_decls() through the header
- Zero test regressions
- emit_c.c is ~1100 lines shorter
</success_criteria>

<output>
After completion, create `.planning/phases/52-emitter-refactoring/52-02-SUMMARY.md`
</output>
