---
phase: 33-value-mutable-captures-optimizer-guards
plan: "03"
type: execute
wave: 3
depends_on: ["33-02"]
files_modified: []
autonomous: true
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - CAPT-04
  - OPT-01
  - OPT-02
  - OPT-03

must_haves:
  truths:
    - "capture_01_immutable compiles and outputs 'Hello from Iron'"
    - "capture_02_mutate compiles and outputs '3'"
    - "capture_03_multi_type compiles and outputs 'point: (42, 20.5)'"
    - "capture_04_loop_snapshot compiles (no hang) and outputs '0' then '3'"
    - "capture_07_callback_arg compiles and outputs '20'"
    - "capture_12_capture_in_branch compiles and outputs 'was true'"
    - "capture_13_capture_in_match compiles and outputs 'not found'"
    - "capture_14_filter_with_capture compiles and outputs '2'"
  artifacts:
    - path: "tests/integration/capture_04_loop_snapshot.iron"
      provides: "Loop variable snapshot test"
      min_lines: 5
    - path: "tests/integration/capture_07_callback_arg.iron"
      provides: "Callback argument test"
      min_lines: 5
    - path: "tests/integration/capture_14_filter_with_capture.iron"
      provides: "Filter with capture test"
      min_lines: 10
  key_links:
    - from: "src/parser/parser.c"
      to: "src/analyzer/typecheck.c"
      via: "Iron_TypeAnnotation with is_func=true flows to resolve_type_annotation"
      pattern: "ann->is_func"
    - from: "src/analyzer/typecheck.c"
      to: "src/lir/emit_c.c"
      via: "IRON_TYPE_FUNC maps to Iron_Closure in C output"
      pattern: "Iron_Closure"
    - from: "src/lir/emit_c.c"
      to: "src/runtime/iron_runtime.h"
      via: "Iron_List_Iron_Closure_create/push/get emitted by codegen, defined in runtime"
      pattern: "Iron_List_Iron_Closure"
---

<objective>
Run all 8 capture integration tests, diagnose and fix any remaining compiler issues, and verify the complete capture test suite is green.

Purpose: Plans 01 and 02 fixed the parser, typecheck resolver, runtime list type, and DCE purity. This plan runs all tests end-to-end. Examples 1-3 should already pass (Phase 32). Examples 4, 7, 12, 13, 14 need the Plan 01+02 fixes. This plan catches any remaining issues in the codegen pipeline (e.g., closure call dispatch for func-typed parameters, match-in-lambda codegen, array indexing with closure call).

Output: All 8 capture tests green, full integration suite passing.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-value-mutable-captures-optimizer-guards/33-CONTEXT.md
@.planning/phases/33-value-mutable-captures-optimizer-guards/33-RESEARCH.md
@.planning/phases/33-value-mutable-captures-optimizer-guards/33-01-SUMMARY.md
@.planning/phases/33-value-mutable-captures-optimizer-guards/33-02-SUMMARY.md

<interfaces>
<!-- The complete pipeline: parser -> typecheck -> HIR -> LIR -> emit_c -> runtime -->

From src/lir/emit_c.c — key patterns for closure codegen:
- IRON_TYPE_FUNC -> "Iron_Closure" (emit_type_to_c)
- IRON_TYPE_ARRAY with IRON_TYPE_FUNC element -> "Iron_List_Iron_Closure" (emit_type_to_c)
- MAKE_CLOSURE emission: malloc env struct, populate fields, assign {fn, env}
- Closure call dispatch: cast .fn to correct signature, call with .env as first arg

From src/runtime/iron_runtime.h (after Plan 02):
- Iron_Closure typedef: {void *env; void (*fn)(void*)}
- Iron_List_Iron_Closure typedef + IRON_LIST_DECL(Iron_Closure, Iron_Closure)
- IRON_LIST_IMPL(Iron_Closure, Iron_Closure) in iron_collections.c

From src/lir/lir_optimize.c (after Plan 02):
- run_dce: MAKE_CLOSURE with capture_count > 0 treated as non-pure (survives DCE)
- Copy-prop Step 1b: scans MAKE_CLOSURE captures, excludes those allocas
- Inliner: skips __lambda_* functions at line 3304
</interfaces>
</context>

<memory_warning>
CRITICAL: When running ironc on any test file, ALWAYS use a timeout.
Use: `timeout 30 ./build/ironc build FILE.iron -o /tmp/output 2>&1`
If any ironc process consumes >4GB, kill it immediately: `kill -9 $(pgrep -f ironc)`
Start with capture_01 (known good) to verify build, then test each new example individually.
</memory_warning>

<tasks>

<task type="auto">
  <name>Task 1: Run all 8 capture tests and fix any remaining issues</name>
  <files></files>
  <read_first>
    .planning/phases/33-value-mutable-captures-optimizer-guards/33-01-SUMMARY.md,
    .planning/phases/33-value-mutable-captures-optimizer-guards/33-02-SUMMARY.md,
    .planning/phases/33-value-mutable-captures-optimizer-guards/33-RESEARCH.md (issues 1-5 and pitfalls),
    src/lir/emit_c.c (MAKE_CLOSURE emission around line 2095-2118, closure call dispatch, emit_type_to_c for IRON_TYPE_FUNC and IRON_TYPE_ARRAY),
    tests/integration/capture_04_loop_snapshot.iron,
    tests/integration/capture_07_callback_arg.iron,
    tests/integration/capture_12_capture_in_branch.iron,
    tests/integration/capture_13_capture_in_match.iron,
    tests/integration/capture_14_filter_with_capture.iron
  </read_first>
  <action>
**Step 1: Build the compiler**
```bash
cd /Users/victor/code/iron-lang/build && cmake --build . 2>&1 | tail -10
```

**Step 2: Verify known-good tests still pass (sanity check)**
```bash
timeout 30 ./build/ironc build tests/integration/capture_01_immutable.iron -o /tmp/cap01 2>&1 && /tmp/cap01
# Expected: "Hello from Iron"
```

**Step 3: Test each new example individually with timeout**

Test in this order (simplest to most complex):
1. capture_12_capture_in_branch (no func-type annotations, simplest new test)
2. capture_13_capture_in_match (match-in-lambda, no func-type annotations)
3. capture_07_callback_arg (standalone func-type parameter)
4. capture_14_filter_with_capture (func-type param + return type + array operations)
5. capture_04_loop_snapshot (array-of-func-type + loop + DCE — LAST because most complex)

For each test:
```bash
timeout 30 ./build/ironc build tests/integration/capture_XX.iron -o /tmp/capXX 2>&1
/tmp/capXX
diff <(/tmp/capXX) tests/integration/capture_XX.expected
```

**Step 4: If any test fails, diagnose and fix**

Common issues to check:
- **Parser error on func()**: Plan 01 fix not applied correctly. Check parser.c iron_parse_type_annotation for IRON_TOK_FUNC handling.
- **"unknown type 'func'"**: Typecheck resolver not handling is_func. Check typecheck.c resolve_type_annotation for is_func branch.
- **Linker error "Iron_List_Iron_Closure_create" undefined**: Missing IRON_LIST_IMPL. Check iron_collections.c.
- **Wrong output from closure call**: Closure dispatch may cast .fn incorrectly. Check emit_c.c closure call emission.
- **Segfault**: Likely env pointer issue — check MAKE_CLOSURE field population in emit_c.c.
- **Process hangs/consumes >4GB**: Parser fix incomplete — kill immediately, check parser.c.

If fixes are needed, modify the minimum files necessary. The `files_modified` list is intentionally empty because this task focuses on testing — but the executor MAY modify source files to fix bugs discovered during testing. Document any source changes in the summary.

**Step 5: Run full integration test suite**
```bash
cd /Users/victor/code/iron-lang && tests/run_tests.sh integration build/ironc 2>&1 | grep -E "PASS|FAIL"
```

All capture tests (01-04, 07, 12, 13, 14) must PASS. No existing tests should regress.

**Step 6: Run unit tests**
```bash
cd /Users/victor/code/iron-lang/build && ctest --output-on-failure 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang && timeout 30 ./build/ironc build tests/integration/capture_01_immutable.iron -o /tmp/cap01 2>&1 && diff <(/tmp/cap01) tests/integration/capture_01_immutable.expected && timeout 30 ./build/ironc build tests/integration/capture_04_loop_snapshot.iron -o /tmp/cap04 2>&1 && diff <(/tmp/cap04) tests/integration/capture_04_loop_snapshot.expected && echo "KEY TESTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `timeout 30 ./build/ironc build tests/integration/capture_01_immutable.iron -o /tmp/cap01 && /tmp/cap01` outputs "Hello from Iron"
    - `timeout 30 ./build/ironc build tests/integration/capture_02_mutate.iron -o /tmp/cap02 && /tmp/cap02` outputs "3"
    - `timeout 30 ./build/ironc build tests/integration/capture_03_multi_type.iron -o /tmp/cap03 && /tmp/cap03` outputs "point: (42, 20.5)"
    - `timeout 30 ./build/ironc build tests/integration/capture_04_loop_snapshot.iron -o /tmp/cap04 && /tmp/cap04` outputs "0\n3"
    - `timeout 30 ./build/ironc build tests/integration/capture_07_callback_arg.iron -o /tmp/cap07 && /tmp/cap07` outputs "20"
    - `timeout 30 ./build/ironc build tests/integration/capture_12_capture_in_branch.iron -o /tmp/cap12 && /tmp/cap12` outputs "was true"
    - `timeout 30 ./build/ironc build tests/integration/capture_13_capture_in_match.iron -o /tmp/cap13 && /tmp/cap13` outputs "not found"
    - `timeout 30 ./build/ironc build tests/integration/capture_14_filter_with_capture.iron -o /tmp/cap14 && /tmp/cap14` outputs "2"
    - No ironc process consumes >4GB during any test
    - Full integration test suite has no regressions: `tests/run_tests.sh integration build/ironc` exits 0
  </acceptance_criteria>
  <done>All 8 capture integration tests compile without hangs and produce correct output. No test regressions. Full integration and unit test suites pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify optimizer guards with verbose output</name>
  <files></files>
  <read_first>
    src/lir/lir_optimize.c (lines 1058-1062 for copy-prop MAKE_CLOSURE guard, lines 3020-3030 for Step 1b capture scan, line 3304 for inliner __lambda_* skip, lines 1229+1289 for DCE capture_count check)
  </read_first>
  <action>
Verify all 3 optimizer requirements (OPT-01, OPT-02, OPT-03) are correctly working with the test suite.

**OPT-01: DCE does not eliminate capturing MAKE_CLOSURE**

Compile capture_02_mutate (simplest capturing closure) with verbose/debug output to see LIR:
```bash
timeout 30 ./build/ironc build tests/integration/capture_02_mutate.iron -o /tmp/cap02_opt --verbose 2>&1 | head -100
```
Verify the generated C code contains MAKE_CLOSURE-equivalent (env struct malloc + assignment). If DCE had eliminated it, the closure body would reference an undefined env.

A more direct check: compile and run. If output is "3", the closure was NOT eliminated by DCE.

**OPT-02: Inliner skips __lambda_* functions**

Already done in Phase 32 (line 3304 of lir_optimize.c). Verify by checking that capture tests produce correct output — if the inliner incorrectly inlined a lambda, the env parameter would be lost and the output would be wrong.

Verify the guard exists:
```bash
grep -n "__lambda_" /Users/victor/code/iron-lang/src/lir/lir_optimize.c | head -5
```
Expected: line ~3304 shows the skip guard.

**OPT-03: Copy-prop and store-load-elim respect heap-box**

capture_02_mutate tests this directly. The `var count` is captured by reference (heap-boxed). If copy-prop forwarded the initial `count = 0` past the closure mutations, the output would be "0" instead of "3".

capture_03_multi_type further tests this — `var x` is captured and mutated to 42 inside the closure context.

Verify both pass:
```bash
timeout 10 /tmp/cap02 && echo "OPT-03 OK for cap02"
timeout 10 /tmp/cap03 && echo "OPT-03 OK for cap03"
```

**Summary check: all optimizer guards verified**
```bash
echo "=== OPT-01: DCE ===" && timeout 10 /tmp/cap04 && echo "PASS (closure not eliminated)"
echo "=== OPT-02: Inliner ===" && grep -c "__lambda_" /Users/victor/code/iron-lang/src/lir/lir_optimize.c && echo "Guard present"
echo "=== OPT-03: Copy-prop ===" && timeout 10 /tmp/cap02 && echo "PASS (heap-box respected)"
```
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang && grep -q "make_closure.capture_count > 0" src/lir/lir_optimize.c && grep -q "__lambda_" src/lir/lir_optimize.c && timeout 10 /tmp/cap02 | grep -q "3" && echo "ALL OPT GUARDS VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - lir_optimize.c contains `make_closure.capture_count > 0` (OPT-01 DCE guard, from Plan 02)
    - lir_optimize.c contains `__lambda_` inliner skip guard (OPT-02, from Phase 32)
    - lir_optimize.c contains copy-prop MAKE_CLOSURE guard (OPT-03, from Phase 32)
    - capture_02_mutate outputs "3" (proves copy-prop and DCE work correctly together)
    - capture_04_loop_snapshot outputs "0\n3" (proves DCE preserves closures in loop)
  </acceptance_criteria>
  <done>All 3 optimizer guards (OPT-01 DCE, OPT-02 inliner, OPT-03 copy-prop) verified working. Capture tests prove closures survive optimization pipeline intact.</done>
</task>

</tasks>

<verification>
1. All 8 capture tests compile and produce correct output (no hangs, no crashes)
2. Full integration test suite green: `tests/run_tests.sh integration build/ironc`
3. Unit tests pass: `cd build && ctest --output-on-failure`
4. OPT-01 verified: capture_04 closure array not eliminated by DCE
5. OPT-02 verified: __lambda_* skip guard in lir_optimize.c
6. OPT-03 verified: capture_02 outputs "3" (heap-box not forwarded by copy-prop)
</verification>

<success_criteria>
- 8/8 capture tests pass: capture_01, 02, 03, 04, 07, 12, 13, 14
- No ironc process hangs or exceeds 4GB
- Full integration suite has zero regressions
- All optimizer guards (OPT-01, OPT-02, OPT-03) confirmed working
- Unit test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/33-value-mutable-captures-optimizer-guards/33-03-SUMMARY.md`
</output>
