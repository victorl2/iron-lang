---
phase: 73-idiomatic-api-polish-showcase-integration-tests
plan: 04
type: execute
wave: 4
depends_on: ["73-03"]
files_modified:
  - tests/integration/raylib/win/win_test.iron
  - tests/integration/raylib/input/input_test.iron
  - tests/integration/raylib/draw2d/draw2d_test.iron
  - tests/integration/raylib/coll/coll_test.iron
  - tests/integration/raylib/tex/tex_test.iron
  - tests/integration/raylib/text/text_test.iron
  - tests/integration/raylib/audio/audio_test.iron
  - tests/integration/raylib/draw3d/draw3d_test.iron
  - tests/integration/raylib/model/model_test.iron
  - tests/integration/raylib/shader/shader_test.iron
  - tests/integration/raylib/math/math_test.iron
  - tests/integration/raylib/file/file_test.iron
  - scripts/test-raylib-integration.sh
autonomous: false
requirements:
  - API-11
  - API-12
must_haves:
  truths:
    - "12 compile-only integration tests exist under tests/integration/raylib/<CATEGORY>/<category>_test.iron"
    - "Each of 12 tests builds on native via `./build/ironc build` (exit 0)"
    - "Each of 12 tests builds on web via `./build/ironc build --target=web` (exit 0) — total 24 compile invocations"
    - "scripts/test-raylib-integration.sh runs all 24 compile invocations + pure-superset guards in a single command"
    - "Pure-superset guarantee verified — pong, game_raylib, hello_raylib_web all build unchanged (API-11)"
  artifacts:
    - path: "tests/integration/raylib/win/win_test.iron"
      provides: "WIN category compile-only test (Window.init/should_close/close + state queries)"
      min_lines: 15
    - path: "tests/integration/raylib/input/input_test.iron"
      provides: "INPUT category compile-only test (Keyboard + Mouse + Gamepad + Gesture)"
      min_lines: 20
    - path: "tests/integration/raylib/draw2d/draw2d_test.iron"
      provides: "DRAW2D category compile-only test (Draw.* primitives)"
      min_lines: 25
    - path: "tests/integration/raylib/coll/coll_test.iron"
      provides: "COLL category compile-only test (Shapes + Ray collisions)"
      min_lines: 20
    - path: "tests/integration/raylib/tex/tex_test.iron"
      provides: "TEX category compile-only test (Image + Texture + Color)"
      min_lines: 20
    - path: "tests/integration/raylib/text/text_test.iron"
      provides: "TEXT category compile-only test (Font + Draw.text + Text.format_i)"
      min_lines: 15
    - path: "tests/integration/raylib/audio/audio_test.iron"
      provides: "AUDIO category compile-only test (Audio + Wave + Sound + Music + AudioStream)"
      min_lines: 20
    - path: "tests/integration/raylib/draw3d/draw3d_test.iron"
      provides: "DRAW3D category compile-only test (Camera3D + Draw.* 3D primitives)"
      min_lines: 25
    - path: "tests/integration/raylib/model/model_test.iron"
      provides: "MODEL category compile-only test (Model + Mesh + Material + Animation + Billboard)"
      min_lines: 25
    - path: "tests/integration/raylib/shader/shader_test.iron"
      provides: "SHADER category compile-only test (Shader load/valid/unload + 4 setter forms)"
      min_lines: 20
    - path: "tests/integration/raylib/math/math_test.iron"
      provides: "MATH category compile-only test (Vector2/3/4, Matrix, Quaternion, scalars)"
      min_lines: 25
    - path: "tests/integration/raylib/file/file_test.iron"
      provides: "FILE category compile-only test (Files + Random)"
      min_lines: 20
    - path: "scripts/test-raylib-integration.sh"
      provides: "CI-lite harness running 12×2 compile invocations + pure-superset guards"
      min_lines: 25
  key_links:
    - from: "scripts/test-raylib-integration.sh"
      to: "tests/integration/raylib/*/*_test.iron"
      via: "./build/ironc build + ./build/ironc build --target=web per file"
      pattern: "for cat in .*CATEGORIES"
    - from: "scripts/test-raylib-integration.sh"
      to: "examples/pong/pong.iron + tests/manual/game_raylib.iron + tests/integration/web/hello_raylib.iron"
      via: "pure-superset guard at end of script"
      pattern: "pong\\.iron|game_raylib\\.iron|hello_raylib\\.iron"
---

<objective>
Ship the 12-file per-category integration test suite under `tests/integration/raylib/<CATEGORY>/<category>_test.iron` + driver script `scripts/test-raylib-integration.sh`. Each test is compile-only (no Window.init + main loop; just exercise the FFI surface). Driver script runs all 12 tests × 2 targets (native + web) = 24 build invocations + pure-superset guards for pong/game_raylib/hello_raylib.

This plan closes API-11 (pure superset verified) + API-12 (per-category integration coverage).

Purpose: Future regressions in the raylib binding surface will now trigger CI-style failure signals from a single command. Phase 74 documentation + post-alpha development both rely on this verification floor.
Output: 12 integration tests + 1 driver script + exit-code verification across 24 compile invocations.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-CONTEXT.md
@.planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-RESEARCH.md
@.planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-03-SUMMARY.md

<!-- Existing smokes → reusable integration test sources for 9 of 12 categories -->
@tests/manual/audio_smoke.iron
@tests/manual/collision_smoke.iron
@tests/manual/draw3d_smoke.iron
@tests/manual/files_smoke.iron
@tests/manual/models_smoke.iron
@tests/manual/raymath_smoke.iron
@tests/manual/shaders_smoke.iron
@tests/manual/text_smoke.iron
@tests/manual/texture_smoke.iron

<!-- Pure-superset consumer targets (API-11) -->
@examples/pong/pong.iron
@tests/manual/game_raylib.iron
@tests/integration/web/hello_raylib.iron

<!-- Canonical per-category test template from 73-RESEARCH.md line 459 -->
<interfaces>
<!-- Per-category test template (compile-only, no main loop) -->
<!-- Source: 73-RESEARCH.md §"Per-category test template" + CONTEXT.md plan 73-04 -->

```iron
-- tests/integration/raylib/audio/audio_test.iron
-- AUDIO category compile-and-link test (API-12 coverage).
-- No runtime assertions: exercise the FFI surface only.
import raylib
func main() -> Int32 {
    Audio.init()
    val w = Wave.load("nonexistent.wav")
    val ok_w = Wave.is_valid(w)
    val s = Sound.load("nonexistent.wav")
    val ok_s = Sound.is_valid(s)
    val m = Music.load("nonexistent.ogg")
    val ok_m = Music.is_valid(m)
    Audio.close()
    return Int32(0)
}
```

Driver script template (from 73-RESEARCH.md lines 431-453):
```bash
#!/bin/bash
# scripts/test-raylib-integration.sh — Phase 73-04
set -e
CATEGORIES=(win input draw2d coll tex text audio draw3d model shader math file)
FAIL=0
for cat in "${CATEGORIES[@]}"; do
    test="tests/integration/raylib/$cat/${cat}_test.iron"
    echo "=== $cat (native) ==="
    ./build/ironc build "$test" || { echo "FAIL: $cat native"; FAIL=1; }
    echo "=== $cat (web) ==="
    ./build/ironc build --target=web "$test" || { echo "FAIL: $cat web"; FAIL=1; }
done
echo "=== pure-superset guard ==="
./build/ironc build examples/pong/pong.iron || FAIL=1
./build/ironc build tests/manual/game_raylib.iron || FAIL=1
./build/ironc build --target=web tests/integration/web/hello_raylib.iron || FAIL=1
exit $FAIL
```

Category reuse map (from 73-RESEARCH.md §"Category smoke reuse map", lines 478-495):
| Category | Source | Need |
| AUDIO    | tests/manual/audio_smoke.iron       | Strip main loop → compile-only subset |
| COLL     | tests/manual/collision_smoke.iron   | Strip main loop |
| DRAW3D   | tests/manual/draw3d_smoke.iron      | Strip main loop |
| FILE     | tests/manual/files_smoke.iron       | Strip main loop |
| MATH     | tests/manual/raymath_smoke.iron     | Strip main loop |
| MODEL    | tests/manual/models_smoke.iron      | Strip main loop |
| SHADER   | tests/manual/shaders_smoke.iron     | Strip main loop |
| TEXT     | tests/manual/text_smoke.iron        | Strip main loop |
| TEX      | tests/manual/texture_smoke.iron     | Strip main loop |
| WIN      | (no existing smoke)                 | **NEW** ~15 lines |
| INPUT    | (no existing smoke)                 | **NEW** ~25 lines |
| DRAW2D   | (no existing smoke)                 | **NEW** ~30 lines |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 12 integration tests under tests/integration/raylib/</name>
  <read_first>
    - .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-RESEARCH.md §"Integration Test Harness" + §"Category smoke reuse map" (lines 418-495)
    - tests/manual/audio_smoke.iron (source for AUDIO integration test)
    - tests/manual/collision_smoke.iron (source for COLL)
    - tests/manual/draw3d_smoke.iron (source for DRAW3D)
    - tests/manual/files_smoke.iron (source for FILE)
    - tests/manual/raymath_smoke.iron (source for MATH)
    - tests/manual/models_smoke.iron (source for MODEL)
    - tests/manual/shaders_smoke.iron (source for SHADER)
    - tests/manual/text_smoke.iron (source for TEXT)
    - tests/manual/texture_smoke.iron (source for TEX)
    - examples/pong/pong.iron (reference for WIN + DRAW2D + INPUT — pattern for new tests)
    - src/stdlib/raylib.iron (authoritative API signatures post Plan 73-02)
  </read_first>
  <files>
    - tests/integration/raylib/win/win_test.iron (new, ~15 lines)
    - tests/integration/raylib/input/input_test.iron (new, ~20 lines)
    - tests/integration/raylib/draw2d/draw2d_test.iron (new, ~25 lines)
    - tests/integration/raylib/coll/coll_test.iron (new, ~20 lines)
    - tests/integration/raylib/tex/tex_test.iron (new, ~20 lines)
    - tests/integration/raylib/text/text_test.iron (new, ~15 lines)
    - tests/integration/raylib/audio/audio_test.iron (new, ~20 lines)
    - tests/integration/raylib/draw3d/draw3d_test.iron (new, ~25 lines)
    - tests/integration/raylib/model/model_test.iron (new, ~25 lines)
    - tests/integration/raylib/shader/shader_test.iron (new, ~20 lines)
    - tests/integration/raylib/math/math_test.iron (new, ~25 lines)
    - tests/integration/raylib/file/file_test.iron (new, ~20 lines)
  </files>
  <action>
    Goal: Create 12 per-category compile-only integration tests. 9 derived from existing smokes, 3 (WIN/INPUT/DRAW2D) authored new.

    Step 0 — Create directory tree:
      ```bash
      for cat in win input draw2d coll tex text audio draw3d model shader math file; do
          mkdir -p "tests/integration/raylib/$cat"
      done
      ```

    Step 1 — Author 3 NEW tests for WIN/INPUT/DRAW2D (no existing smoke sources):

      (a) **tests/integration/raylib/win/win_test.iron** (~15 lines):
        ```
        -- WIN category compile-and-link test (API-12 coverage).
        -- Exercises: Window lifecycle + state queries + set_target_fps + FPS + frame time.
        import raylib

        func main() -> Int32 {
            Window.init(Int32(320), Int32(240), "win_test")
            Window.set_target_fps(Int32(60))
            val ready = Window.is_ready()
            val w = Window.get_screen_width()
            val h = Window.get_screen_height()
            val fps = Window.get_fps()
            val dt = Window.get_frame_time()
            val t = Window.get_time()
            Window.close()
            return Int32(0)
        }
        ```

      (b) **tests/integration/raylib/input/input_test.iron** (~20 lines):
        ```
        -- INPUT category compile-and-link test (API-12 coverage).
        -- Exercises: Keyboard + Mouse + Gamepad + Gesture FFI surface.
        import raylib

        func main() -> Int32 {
            Window.init(Int32(320), Int32(240), "input_test")
            val kp = Keyboard.is_pressed(KeyboardKey.SPACE)
            val kd = Keyboard.is_down(KeyboardKey.A)
            val ku = Keyboard.is_released(KeyboardKey.S)
            val k = Keyboard.get_key_pressed()
            val mp = Mouse.is_button_pressed(MouseButton.LEFT)
            val mpos = Mouse.get_position()
            val mwheel = Mouse.get_wheel_move()
            val gavail = Gamepad.is_available(Int32(0))
            val gname = Gamepad.get_name(Int32(0))
            val gaxis = Gamepad.get_axis_movement(Int32(0), GamepadAxis.LEFT_X)
            val gtouch = Touch.get_point_count()
            val gesture = Gestures.get_detected()
            Window.close()
            return Int32(0)
        }
        ```
        (Adjust `Keyboard.*` / `Mouse.*` / `Gamepad.*` / `Touch.*` / `Gestures.*` namespace names to match current raylib.iron; grep for exact forms first.)

      (c) **tests/integration/raylib/draw2d/draw2d_test.iron** (~25 lines):
        ```
        -- DRAW2D category compile-and-link test (API-12 coverage).
        -- Exercises: Draw.begin/end + clear + pixels/lines/circles/rects/triangles/splines.
        import raylib

        func main() -> Int32 {
            Window.init(Int32(320), Int32(240), "draw2d_test")
            Draw.begin()
            Draw.clear(RAYWHITE)
            Draw.pixel(Int32(10), Int32(10), RED)
            Draw.line(Int32(0), Int32(0), Int32(100), Int32(100), BLUE)
            Draw.line_ex(Vector2.of(Float32(0), Float32(50)), Vector2.of(Float32(100), Float32(50)), Float32(2.0), GREEN)
            Draw.circle(Int32(50), Int32(50), Float32(20.0), YELLOW)
            Draw.circle_lines(Int32(100), Int32(100), Float32(15.0), ORANGE)
            Draw.rectangle(Int32(150), Int32(150), Int32(50), Int32(50), PURPLE)
            Draw.rectangle_lines(Int32(200), Int32(200), Int32(30), Int32(30), PINK)
            Draw.rectangle_rounded(Rectangle.of(Float32(10), Float32(100), Float32(50), Float32(30)), Float32(0.3), Int32(8), LIME)
            Draw.triangle(Vector2.of(Float32(0), Float32(0)), Vector2.of(Float32(20), Float32(0)), Vector2.of(Float32(10), Float32(20)), MAROON)
            Draw.poly(Vector2.of(Float32(100), Float32(100)), Int32(6), Float32(20.0), Float32(0.0), BEIGE)
            Draw.end()
            Window.close()
            return Int32(0)
        }
        ```
        (Adjust `Draw.*` names + `Rectangle.of` / `Vector2.of` usage to match current API post 73-02.)

    Step 2 — Author 9 tests derived from existing smokes (strip main loop; preserve FFI calls):

      For each category C ∈ {audio, coll, draw3d, file, math, model, shader, text, tex}:

      (a) Copy the structural skeleton (imports, `func main() -> Int32`, top-level namespace init/close) from the matching smoke. Replace `tests/manual/<cat>_smoke.iron` filename reference with `tests/integration/raylib/<cat>/<cat>_test.iron`.

      (b) Drop everything between `Draw.begin()` and `Draw.end()` that's main-loop iteration-dependent. Keep only:
        - Namespace init (Audio.init, Window.init — needed for FFI linkage even though runtime never reaches it, because ironc/clang/emcc resolve the symbols at link time).
        - Resource load calls (Wave.load, Sound.load, Music.load, Image.load, Texture.load, Font.load, Shader.load, Model.load, etc.).
        - A sample of 5-15 per-category API calls exercising distinct FFI shapes (struct-in, struct-return, [UInt8] in/out, enum, tuple, etc.).
        - Namespace close (Audio.close, Window.close).
        - `return Int32(0)`.

      (c) Remove all `-- ── <CAT>-NN: …` tag comments — the integration test is compile-only, no tagged-section auditing needed.

      (d) Remove all `print(...)` / `println(...)` calls — compile-only doesn't need output.

      (e) Each file: ~15-30 lines.

      Example for **tests/integration/raylib/audio/audio_test.iron** (~20 lines):
        ```
        -- AUDIO category compile-and-link test (API-12 coverage).
        -- Exercises: Audio device + Wave + Sound + Music + AudioStream FFI surface.
        import raylib

        func main() -> Int32 {
            Audio.init()
            val ready = Audio.is_ready()
            Audio.set_master_volume(Float32(0.5))
            val w = Wave.load("tests/assets/bounce.wav")
            val ok_w = Wave.is_valid(w)
            val s = Sound.load("tests/assets/bounce.wav")
            val ok_s = Sound.is_valid(s)
            Sound.play(s)
            Sound.set_volume(s, Float32(0.8))
            val m = Music.load("tests/assets/loop.ogg")
            val ok_m = Music.is_valid(m)
            Music.play(m)
            val stream = AudioStream.load(UInt32(44100), UInt32(16), UInt32(2))
            val ok_st = AudioStream.is_valid(stream)
            Sound.unload(s)
            Wave.unload(w)
            Music.unload(m)
            AudioStream.unload(stream)
            Audio.close()
            return Int32(0)
        }
        ```

    Step 3 — Per-file build verification (native only at this step; web batch in Task 2):
      ```bash
      for cat in win input draw2d coll tex text audio draw3d model shader math file; do
          ./build/ironc build "tests/integration/raylib/$cat/${cat}_test.iron" || echo "FAIL: $cat native"
      done
      ```
      All 12 exits 0. If any fail, fix the test (usually: wrong API method name — consult current src/stdlib/raylib.iron for exact spelling).

    Step 4 — Commit:
      `git add tests/integration/raylib/ && git commit -m "test(73-04): add 12 per-category compile-only integration tests (API-12)"`
  </action>
  <verify>
    <automated>for cat in win input draw2d coll tex text audio draw3d model shader math file; do ./build/ironc build "tests/integration/raylib/$cat/${cat}_test.iron" || { echo "FAIL: $cat"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/integration/raylib/win/win_test.iron tests/integration/raylib/input/input_test.iron tests/integration/raylib/draw2d/draw2d_test.iron tests/integration/raylib/coll/coll_test.iron tests/integration/raylib/tex/tex_test.iron tests/integration/raylib/text/text_test.iron tests/integration/raylib/audio/audio_test.iron tests/integration/raylib/draw3d/draw3d_test.iron tests/integration/raylib/model/model_test.iron tests/integration/raylib/shader/shader_test.iron tests/integration/raylib/math/math_test.iron tests/integration/raylib/file/file_test.iron` — all 12 files exist
    - `for cat in win input draw2d coll tex text audio draw3d model shader math file; do ./build/ironc build "tests/integration/raylib/$cat/${cat}_test.iron" || exit 1; done` exits 0 (all 12 native builds GREEN)
    - Each file has `import raylib` + `func main() -> Int32` + `return Int32(0)`: `grep -l "import raylib" tests/integration/raylib/*/*_test.iron | wc -l` returns 12
    - Commit exists with subject starting `test(73-04):`
  </acceptance_criteria>
  <done>
    12 per-category integration tests exist and all pass native build. API-12 Iron-side surface coverage established.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create driver script scripts/test-raylib-integration.sh + web target matrix</name>
  <read_first>
    - .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-RESEARCH.md §"Integration Test Harness" (lines 418-453)
    - .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-RESEARCH.md §"Web Target Build Invocation" (lines 256-289) — `dist/web/` overwrite semantics
    - Task 1 outcome: 12 native builds GREEN
    - examples/pong/pong.iron + tests/manual/game_raylib.iron + tests/integration/web/hello_raylib.iron (pure-superset targets)
  </read_first>
  <files>
    - scripts/test-raylib-integration.sh (new, executable)
  </files>
  <action>
    Goal: Author the CI-lite driver script that runs all 12 tests × 2 targets + pure-superset guards + exits with aggregate pass/fail. Per 73-CONTEXT.md Open Q6 + 73-RESEARCH.md line 279: web target overwrites `dist/web/` per invocation; script accepts exit-code-only semantics (no artifact retention between tests).

    Step 0 — Ensure scripts/ dir exists:
      ```bash
      mkdir -p scripts
      ```

    Step 1 — Author scripts/test-raylib-integration.sh:
      ```bash
      #!/bin/bash
      # scripts/test-raylib-integration.sh
      # Phase 73-04 driver: 12 integration tests × 2 targets + pure-superset guards.
      # Each --target=web invocation overwrites dist/web/; this harness uses
      # exit-code-only semantics (no artifact retention).

      set -u
      CATEGORIES=(win input draw2d coll tex text audio draw3d model shader math file)
      FAIL=0

      echo "================================================================"
      echo " Phase 73-04 raylib integration test matrix"
      echo " 12 categories × 2 targets = 24 build invocations"
      echo " + 3 pure-superset guards (pong, game_raylib, hello_raylib_web)"
      echo "================================================================"

      for cat in "${CATEGORIES[@]}"; do
          test="tests/integration/raylib/$cat/${cat}_test.iron"
          if [ ! -f "$test" ]; then
              echo "MISSING: $test"
              FAIL=1
              continue
          fi
          printf "%-10s native ... " "$cat"
          if ./build/ironc build "$test" > /dev/null 2>&1; then
              echo "PASS"
          else
              echo "FAIL"
              FAIL=1
          fi
          printf "%-10s web    ... " "$cat"
          if ./build/ironc build --target=web "$test" > /dev/null 2>&1; then
              echo "PASS"
          else
              echo "FAIL"
              FAIL=1
          fi
      done

      echo "----------------------------------------------------------------"
      echo " Pure-superset guards (API-11)"
      echo "----------------------------------------------------------------"

      printf "pong        native ... "
      if ./build/ironc build examples/pong/pong.iron > /dev/null 2>&1; then
          echo "PASS"
      else
          echo "FAIL"; FAIL=1
      fi

      printf "game_raylib native ... "
      if ./build/ironc build tests/manual/game_raylib.iron > /dev/null 2>&1; then
          echo "PASS"
      else
          echo "FAIL"; FAIL=1
      fi

      printf "hello_raylib web   ... "
      if ./build/ironc build --target=web tests/integration/web/hello_raylib.iron > /dev/null 2>&1; then
          echo "PASS"
      else
          echo "FAIL"; FAIL=1
      fi

      echo "================================================================"
      if [ "$FAIL" -eq 0 ]; then
          echo " ALL 27 BUILDS GREEN — API-11 + API-12 closed"
      else
          echo " FAILURES DETECTED — see above"
      fi
      echo "================================================================"

      exit $FAIL
      ```

    Step 2 — Make executable:
      ```bash
      chmod +x scripts/test-raylib-integration.sh
      ```

    Step 3 — Run the full matrix:
      ```bash
      ./scripts/test-raylib-integration.sh
      echo "exit code: $?"
      ```
      Expected: all 27 builds PASS (24 integration + 3 pure-superset), exit code 0.

    Step 4 — Troubleshoot any FAIL entries:
      - native FAIL on `<cat>`: check test file's API usage vs current raylib.iron. Usually: typo in method name.
      - web FAIL on `<cat>` but native PASS: check for symbol unresolvable at wasm link; verify iron_raylib.c has the shim body (not just prototype in .h).
      - pure-superset FAIL: Plan 73-02 polish broke a pong/game_raylib/hello_raylib call site — revert the offending polish commit and document as gap.

      Fix any failure iteratively; rebuild; re-run.

    Step 5 — Commit:
      `git add scripts/test-raylib-integration.sh && git commit -m "test(73-04): add raylib integration test driver (12×2 + pure-superset guards)"`
  </action>
  <verify>
    <automated>./scripts/test-raylib-integration.sh</automated>
  </verify>
  <acceptance_criteria>
    - `ls scripts/test-raylib-integration.sh` — file exists
    - `test -x scripts/test-raylib-integration.sh` — executable bit set
    - `./scripts/test-raylib-integration.sh` exits 0 (all 27 builds PASS)
    - Script contains `CATEGORIES=(win input draw2d coll tex text audio draw3d model shader math file)` array: `grep -c "win input draw2d coll tex text audio draw3d model shader math file" scripts/test-raylib-integration.sh` returns >= 1
    - Script contains pure-superset guards: `grep -c "pong.iron\|game_raylib.iron\|hello_raylib.iron" scripts/test-raylib-integration.sh` returns >= 3
    - Commit exists with subject starting `test(73-04):`
  </acceptance_criteria>
  <done>
    Single-command CI-lite harness validates the full Phase 73 deliverable surface. Future raylib binding regressions trigger failure signals from this script.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 73 closure verification + SUMMARY (checkpoint)</name>
  <files>
    - .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-04-SUMMARY.md (new)
    - .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-SUMMARY.md (new — phase-level aggregate)
  </files>
  <action>
    **CHECKPOINT: human verification before Phase 73 closes + v2.0.0-alpha milestone declares complete.**

    What-built: Plan 73-04 closed API-11 + API-12: 12 per-category integration tests built on both native + web targets; scripts/test-raylib-integration.sh drives the full 24-build matrix + 3 pure-superset guards in a single command. Phase 73 and v2.0.0-alpha milestone complete.

    How-to-verify (the human runs these):

    Step 1 — Run the full matrix:
      ```bash
      ./scripts/test-raylib-integration.sh
      echo "exit code: $?"
      ```
      Expected: all 27 builds PASS, exit code 0.

    Step 2 — Capture final byte sizes for all consumers (post-Phase-73 baselines for Phase 74):
      ```bash
      ./build/ironc build examples/pong/pong.iron && stat -f%z ./pong
      ./build/ironc build tests/manual/game_raylib.iron && stat -f%z ./game_raylib
      ./build/ironc build examples/raylib_showcase/showcase.iron && stat -f%z ./raylib_showcase
      ./build/ironc build examples/rotating_cube/rotating_cube.iron && stat -f%z ./rotating_cube
      ./build/ironc build examples/model_viewer/model_viewer.iron && stat -f%z ./model_viewer
      ./build/ironc build examples/post_fx/post_fx.iron && stat -f%z ./post_fx
      ```

    Step 3 — Write .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-04-SUMMARY.md capturing:
      - **12 integration tests** — per-category list with line counts
      - **Driver script** — `scripts/test-raylib-integration.sh` full-matrix outcome
      - **API-11 pure-superset verification** — pong + game_raylib + hello_raylib_web all build unchanged; byte-size table
      - **API-12 category coverage** — 12/12 in-scope categories covered
      - **v2.0.0-alpha milestone status** — all 183 requirements closed
      - **Phase 73 aggregate** — 4 plans, N commits, M tasks, requirement IDs closed: API-01..13 (except API-08/09/11 pre-closed)
      - **Byte-size baseline table** — pong, game_raylib, showcase, rotating_cube, model_viewer, post_fx

    Step 4 — Write .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-SUMMARY.md (phase-level aggregate SUMMARY — one file across all 4 plans) with:
      - Phase goal recap
      - All plans + their closures
      - Remaining residual deferrals forwarded to Phase 74+ or post-alpha
      - v2.0.0-alpha milestone close statement

    Resume signal: Type "approved" or describe failures.

    All GREEN + SUMMARY + phase-level SUMMARY complete → approve.
    Any FAIL → describe and hand back.
  </action>
  <verify>
    <automated>test -f .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-04-SUMMARY.md && test -f .planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-SUMMARY.md && ./scripts/test-raylib-integration.sh</automated>
  </verify>
  <acceptance_criteria>
    - 73-04-SUMMARY.md exists with line count >= 50
    - 73-SUMMARY.md exists (phase-level aggregate) with line count >= 60
    - `./scripts/test-raylib-integration.sh` exits 0 (all 27 builds GREEN)
    - Human reviewer has typed "approved" in the resume signal
  </acceptance_criteria>
  <done>
    Phase 73 CLOSED. v2.0.0-alpha milestone CLOSED at 183/183 requirements.
  </done>
</task>

</tasks>

<verification>
After Task 3 approval:
- 12 integration tests under tests/integration/raylib/*/*_test.iron all build native + web (24 builds GREEN)
- scripts/test-raylib-integration.sh runs single-command 27-build matrix, exits 0
- Pure-superset guarantee (API-11): pong, game_raylib, hello_raylib_web all build unchanged
- 73-04-SUMMARY.md captures all evidence
- 73-SUMMARY.md phase-level aggregate documents milestone close
- v2.0.0-alpha milestone complete (183/183 requirements closed)
</verification>

<success_criteria>
Plan 73-04 complete when:
- Task 1: 12 integration tests exist + all 12 native builds PASS
- Task 2: scripts/test-raylib-integration.sh runs full 27-build matrix and exits 0
- Task 3: SUMMARY.md + 73-SUMMARY.md written, all guards GREEN
- API-11 verified (pure superset holds)
- API-12 closed (per-category coverage established)
- Phase 73 CLOSED + v2.0.0-alpha milestone CLOSED
</success_criteria>

<output>
After Task 3 approval, create:
- `.planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-04-SUMMARY.md` — plan closure record
- `.planning/phases/73-idiomatic-api-polish-showcase-integration-tests/73-SUMMARY.md` — phase-level aggregate SUMMARY spanning all 4 plans

Mark Phase 73 CLOSED in ROADMAP.md + STATE.md + REQUIREMENTS.md (API-01..13 all Complete).

v2.0.0-alpha milestone CLOSED. Forward any residual deferrals to Phase 74+ or post-alpha scope.
</output>
