---
phase: 67-correctness-fixes-+-crash-canaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, REG-02]
must_haves:
  truths:
    - "67-AUDIT-STATUS.md lists every top-20 audit row with status (OPEN / DONE) + evidence (commit SHA or grep output)"
    - "Every OPEN row has a pointer to the Phase 67 plan that will close it (01 through 08)"
    - "Wasm post-merge re-audit has been performed across all 6 Wasm files and any new H-severity rows are enumerated in a dedicated section"
    - "Downstream Phase 67 plans have a single source-of-truth document to read before touching any audit row"
  artifacts:
    - path: ".planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md"
      provides: "Top-20 status verification table + Wasm re-audit findings + plan assignment"
      contains: "# Phase 67 Audit Status"
  key_links:
    - from: "67-AUDIT-STATUS.md"
      to: ".planning/research/CORRECTNESS-AUDIT.md"
      via: "Row numbers 1-20 cross-referenced by rank"
      pattern: "Rank \\d+"
    - from: "67-AUDIT-STATUS.md"
      to: ".planning/phases/66-structural-protections-linux-release-ci/66-*-SUMMARY.md"
      via: "commit SHA per DONE row"
      pattern: "commit `[0-9a-f]{7}"
---

<objective>
Produce `.planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` — a verification artifact that maps every top-20 CORRECTNESS-AUDIT.md row to its current state (OPEN / DONE) with concrete evidence (commit SHA for DONE rows, grep output + file:line for OPEN rows), AND performs a re-audit of the post-Phase-65 WebAssembly files for any new H-severity blind-cast / enum-switch / malloc-without-check rows not covered by the original audit.

**This plan is NO CODE. Zero source edits.** Every downstream Phase 67 plan (02 through 08) reads this document before touching any audit row, so downstream work never re-fixes a closed row or misses a row the original audit predated.

Purpose: Phase 66 closed 13 of the top-20 rows (ranks 5-13, 16, 17, 18, 20 — all blind-cast rewrites + the iron_net.c row 24 fix + the collect_mono_enums/elem_suffix/emit_type_to_c switch fixes). Without this verification the planner + downstream executors have no single grep-able source of truth that tells them which rows remain open vs. which are already shipped. Producing this artifact saves every downstream plan from re-reading the audit and Phase 66 summaries to infer their own row assignment.

Output: One markdown file with three sections — (1) top-20 status table (20 rows), (2) Wasm re-audit findings (0 or more new H rows per Wasm file), (3) Plan 02-08 assignment table mapping each OPEN row to the plan that will close it.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/CORRECTNESS-AUDIT.md
@.planning/phases/67-correctness-fixes-+-crash-canaries/67-CONTEXT.md
@.planning/phases/67-correctness-fixes-+-crash-canaries/67-RESEARCH.md
@.planning/phases/66-structural-protections-linux-release-ci/66-01-SUMMARY.md
@.planning/phases/66-structural-protections-linux-release-ci/66-02-SUMMARY.md
@.planning/phases/66-structural-protections-linux-release-ci/66-03-SUMMARY.md
@.planning/phases/66-structural-protections-linux-release-ci/66-04-SUMMARY.md
@.planning/phases/66-structural-protections-linux-release-ci/66-05-SUMMARY.md

<interfaces>
<!-- No code interfaces — this plan produces a markdown verification document only. -->
<!-- Cross-reference format for DONE rows: -->

| Rank | File:Line | Status | Evidence |
|------|-----------|--------|----------|
| 5 | typecheck.c:1360 | DONE | Phase 66-03 commit d0070b7 — IRON_NODE_ASSERT_KIND(callee_sym->decl_node, IRON_NODE_OBJECT_DECL) at typecheck.c:1380; fixture blind_cast_type_sym_decl.iron |
| 1 | iron_runtime.h:497 | OPEN | grep returns 0 hits for "iron_oom_abort\|if (!.*realloc" in IRON_LIST_IMPL _push body; macro body at iron_runtime.h:494-500 unchanged from Phase 65 baseline |

<!-- Wasm re-audit section format: -->

## Wasm Re-Audit (post-Phase-65)

Files re-audited (all 6 post-merge WebAssembly files):
- src/lir/emit_web.c                   (480 lines)
- src/lir/web_main_loop_split.c        (664 lines)
- src/analyzer/web_await_check.c       (231 lines)
- src/analyzer/web_top_level_loader_check.c (212 lines)
- src/cli/build_web.c                  (1024 lines)
- src/stdlib/iron_time_web.c           (122 lines)

Audit dimensions applied (same 6 as Phase 65):
1. Blind casts (AUDIT-01): grep `\(Iron_.*\*\)` without preceding kind check
2. Enum switch exhaustiveness (AUDIT-02): switch over Iron_*Kind without exhaustive cases or -Wswitch-enum opt-out comment
3. Null safety (AUDIT-03): pointer deref without NULL guard
4. Integer safety (AUDIT-05): unchecked atoi/atol, signed overflow, size calcs
5. Allocation error handling (AUDIT-06): malloc/realloc/strdup/calloc without NULL check
6. Arena lifetimes (AUDIT-04): cross-arena storage in stb_ds or vice-versa

New H findings (if any):
| File | Line | Dimension | Description | Suggested Fix | Target Plan |
|------|------|-----------|-------------|---------------|-------------|
| (none) | — | — | — | — | — |

<!-- Plan assignment table format: -->

## Plan Assignment for OPEN Rows

| Rank | File:Line | Plan | Rationale |
|------|-----------|------|-----------|
| 1 | iron_runtime.h:497 | 67-02 | H-severity runtime macro OOM — lands with iron_oom_abort helper |
| 2 | iron_runtime.h:640-641 | 67-02 | H-severity runtime macro OOM — same |
| 3 | emit_c.c:3159-3168 | 67-02 | H-severity generated-C OOM — emit iron_oom_abort call in codegen |
| 4 | emit_c.c:3179-3192 | 67-02 | H-severity generated-C OOM — same |
| 14 | comptime.c:410-426 | 67-03 | Integer-overflow arithmetic — __builtin_add_overflow |
| 15 | comptime.c:493 | 67-03 | INT64_MIN negation — explicit check |
| 19 | parser.c:2585 | 67-03 | atoi → strtol for enum variant values |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify top-20 audit rows and build status table</name>
  <read_first>
    - .planning/research/CORRECTNESS-AUDIT.md (lines 268-295 — the top-20 table)
    - .planning/phases/66-structural-protections-linux-release-ci/66-03-SUMMARY.md (PROT-04 H-severity rewrites — ranks 5-12 evidence)
    - .planning/phases/66-structural-protections-linux-release-ci/66-05-SUMMARY.md (M-severity walkthrough — rank 16 iron_net.c evidence)
    - .planning/phases/66-structural-protections-linux-release-ci/66-02-SUMMARY.md (enum-switch -Werror — ranks 13, 17, 20 evidence)
    - .planning/phases/67-correctness-fixes-+-crash-canaries/67-RESEARCH.md (lines 265-290 — researcher's status table draft with commit SHAs)
    - src/runtime/iron_runtime.h (lines 480-680 — verify IRON_LIST/MAP macros are unchanged)
    - src/lir/emit_c.c (lines 3155-3200 — verify HEAP_ALLOC + RC_ALLOC malloc sites)
    - src/analyzer/typecheck.c (search for PROT-04 rewrite (rank — confirms Phase 66-03 landed)
    - src/analyzer/resolve.c (search for _Static_assert(sizeof(Iron_MethodCallExpr) — confirms Phase 66-03 landed)
    - src/parser/parser.c (lines 2580-2590 — verify atoi site unchanged at line 2585)
    - src/comptime/comptime.c (lines 395-500 — verify int64 arithmetic and INT64_MIN negation unchanged)
    - src/stdlib/iron_net.c (lines 585-620 — verify iron_net.c row 24 local-buffer fix landed)
  </read_first>
  <files>.planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md</files>
  <action>
Create a new file `.planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` with the following structure:

```markdown
# Phase 67 Audit Status

**Created:** 2026-04-13
**Purpose:** Single source of truth for which CORRECTNESS-AUDIT.md top-20 rows remain OPEN after Phase 66 and which are DONE. Every Phase 67 plan (67-02 through 67-08) reads this document before touching any audit row.

## Top-20 Status Table

| Rank | File:Line (post-Phase-66 drift) | Dimension | Status | Evidence | Target Plan |
|------|----------------------------------|-----------|--------|----------|-------------|
| 1 | src/runtime/iron_runtime.h:497 | Null + Alloc | OPEN | IRON_LIST_IMPL _push macro body unchanged; realloc result still dereferenced without NULL check on line 499 | 67-02 |
| 2 | src/runtime/iron_runtime.h:640-641 | Null + Alloc | OPEN | IRON_MAP_IMPL _put macro body unchanged; two realloc calls both dereferenced without NULL check on lines 643-644 | 67-02 |
| 3 | src/lir/emit_c.c:3159-3168 | Null + Alloc | OPEN | IRON_LIR_HEAP_ALLOC case still emits `T *_vN = (T *)malloc(sizeof(T))` on line 3168 followed immediately by `*_vN = val;` with no NULL check | 67-02 |
| 4 | src/lir/emit_c.c:3179-3192 | Null + Alloc | OPEN | IRON_LIR_RC_ALLOC case at line 3192 same unchecked malloc + dereference pattern | 67-02 |
| 5 | src/analyzer/typecheck.c:~1380 | Blind Cast | DONE — Phase 66-03 | commit d0070b7 (fix(66-03): PROT-04 typecheck.c rewrites ranks 5/6/11a/11b); grep `"IRON_NODE_ASSERT_KIND(callee_sym->decl_node, IRON_NODE_OBJECT_DECL)"` → 1 hit; fixture `tests/integration/blind_cast_type_sym_decl.iron` exists | — |
| 6 | src/analyzer/typecheck.c:~1805 | Blind Cast | DONE — Phase 66-03 | Same commit d0070b7; grep `"IRON_NODE_ASSERT_KIND(sym->decl_node, IRON_NODE_OBJECT_DECL)"` → 1 hit; same fixture covers both ranks 5 + 6 | — |
| 7 | src/analyzer/resolve.c:~728 | Blind Cast | DONE — Phase 66-03 | commit 4e19a9d (fix(66-03): PROT-04 resolve.c rewrites ranks 7/8/10a/10b); grep `"_Static_assert(sizeof(Iron_MethodCallExpr) <= sizeof(Iron_EnumConstruct)"` → 1 hit at resolve.c line ~730; fixture `tests/integration/enum_construct_reinterpret.iron` exists | — |
| 8 | src/analyzer/resolve.c:~745 | Blind Cast | DONE — Phase 66-03 | Same commit 4e19a9d; grep `"_Static_assert(sizeof(Iron_FieldAccess) <= sizeof(Iron_EnumConstruct)"` → 1 hit; same fixture covers both ranks 7 + 8 | — |
| 9 | src/hir/hir_lower.c:109 (layout assumption) | Blind Cast | DONE — Phase 66-01 + 66-03 | Phase 66-01 moved Iron_ExprNode to ast.h with _Static_assert prefix enforcement (115 checks, 23 types); Phase 66-03 added runtime fixture `tests/integration/blind_cast_expr_common_layout.iron` exercising 12 expression kinds | — |
| 10 | src/analyzer/resolve.c:~236,~251 | Blind Cast | DONE — Phase 66-03 | commit 4e19a9d; grep `"PROT-04 rewrite (rank 10"` in resolve.c → 2 hits; fixture `tests/integration/blind_cast_owner_decl.iron` exists | — |
| 11 | src/analyzer/typecheck.c:3064 + ~2725 | Blind Cast | DONE — Phase 66-03 | commit d0070b7; grep `"Iron_ExprNode \*expr_node = (Iron_ExprNode \*)rs->value"` → 1 hit (rank 11a); grep `"IRON_NODE_ASSERT_KIND(rs->value, IRON_NODE_INT_LIT)"` → 1 hit (rank 11b); fixture `tests/integration/blind_cast_expr_resolved_type.iron` exists | — |
| 12 | src/analyzer/escape.c:~251 | Blind Cast | DONE — Phase 66-03 | commit 9eb993a; grep `"IRON_NODE_ASSERT_KIND(ls->expr, IRON_NODE_IDENT)"` → 1 hit; fixture `tests/integration/blind_cast_leak_ident.iron` exists | — |
| 13 | src/hir/hir_to_lir.c:~600-697 | Enum Switch | DONE — Phase 66-02 | -Werror=switch-enum forced exhaustive coverage; collect_mono_enums_node now handles FOR / DEFER / LAMBDA / INDEX / SLICE / ARRAY_LIT / CONSTRUCT cases (verify via grep in hir_to_lir.c lines 600-697 for those IRON_NODE_ values) | — |
| 14 | src/comptime/comptime.c:410-426 | Integer Safety | OPEN | int64 +/-/* still direct: `lv->as_int + rv->as_int` on line 410, `- rv->as_int` on 411, `* rv->as_int` on 412 — no __builtin_*_overflow wrapper | 67-03 |
| 15 | src/comptime/comptime.c:493 | Integer Safety | OPEN | `-operand->as_int` on line 493 — no INT64_MIN check | 67-03 |
| 16 | src/stdlib/iron_net.c:~594 | Blind Cast | DONE — Phase 66-05 | commit abfeff5 (fix(66-05): iron_net.c const-strip with local heap buffer); grep `"local_recv_buf = (uint8_t \*)malloc"` → 1 hit in iron_net.c lines 580-620 | — |
| 17 | src/hir/hir_to_lir.c:~1133-1159 | Enum Switch | DONE — Phase 66-02 | -Werror=switch-enum forced elem_suffix switch to cover INT8/16/32/64, UINT*, FLOAT32/64, ENUM (verify via grep in hir_to_lir.c near elem_suffix function) | — |
| 18 | src/analyzer/resolve.c:~728-752 layout | Blind Cast | DONE — Phase 66-03 | Same commit 4e19a9d as ranks 7/8; `_Static_assert(sizeof(Iron_MethodCallExpr/FieldAccess) <= sizeof(Iron_EnumConstruct))` is literally the layout assertion this audit row called for | — |
| 19 | src/parser/parser.c:2585 | Integer Safety | OPEN | `v->explicit_value = (int)atoi(num->value);` on line 2585 unchanged; no strtol / ERANGE handling | 67-03 |
| 20 | src/lir/emit_helpers.c:~115-206 | Enum Switch | DONE — Phase 66-02 | emit_type_to_c became exhaustive over Iron_TypeKind (IRON_TYPE_ERROR + IRON_TYPE_TUPLE added); confirmed by Phase 66-02 SUMMARY "confirmed by Phase 66 verification: IRON_TYPE_ERROR + IRON_TYPE_TUPLE added; -Wswitch-enum passes" | — |

**Summary:** 13 DONE (ranks 5-13, 16, 17, 18, 20), 7 OPEN (ranks 1, 2, 3, 4, 14, 15, 19).

## Wasm Re-Audit (post-Phase-65)

Phase 65 CORRECTNESS-AUDIT.md pre-dated the WebAssembly target merge. This section re-audits the 6 post-merge Wasm files against the same 6 dimensions as the original audit.

Files re-audited:
- src/lir/emit_web.c (480 lines)
- src/lir/web_main_loop_split.c (664 lines)
- src/analyzer/web_await_check.c (231 lines)
- src/analyzer/web_top_level_loader_check.c (212 lines)
- src/cli/build_web.c (1024 lines)
- src/stdlib/iron_time_web.c (122 lines)

Dimensions applied (same 6 as Phase 65):
1. **Blind casts (AUDIT-01):** `grep -n '(Iron_.*\*)'` in each file, inspect preceding lines for kind check
2. **Enum switch (AUDIT-02):** `grep -n 'switch.*kind\|switch.*op\|switch.*type'` for missing cases / opt-out comments
3. **Null safety (AUDIT-03):** `grep -n '->[a-z]\|\*[a-z_]'` for unguarded deref in syscall/alloc paths
4. **Integer safety (AUDIT-05):** `grep -n 'atoi\|atol\|atof\|size_t.*int\|int.*size_t'` for overflow risk
5. **Allocation errors (AUDIT-06):** `grep -n 'malloc\|realloc\|calloc\|strdup'` (non-arena) for missing NULL check
6. **Arena lifetimes (AUDIT-04):** `grep -n 'stb_ds\|arrput\|shput\|hmput'` for cross-arena storage

### New H-severity findings

| File | Line | Dimension | Description | Suggested Fix | Target Plan |
|------|------|-----------|-------------|---------------|-------------|
| _(populate from grep results — most likely empty because Wasm files were written post-Phase-66 with PROT-01..04 already live, so any blind casts in them should already use IRON_NODE_ASSERT_KIND. If the grep finds bare `(Iron_*` casts without kind guards, add them here as new rows.)_ | | | | | |

### New M-severity findings (informational — not scoped for Phase 67 unless H)

_(If grep finds enough M-severity rows to be worth documenting, list them here with severity M; Phase 67 does NOT have to fix M rows in the Wasm files unless they happen to fall under FIX-02's arena walkthrough scope — see 67-05/06/07 for that scope.)_

## Plan Assignment for OPEN Rows

| Rank | File:Line | Plan | Rationale |
|------|-----------|------|-----------|
| 1 | src/runtime/iron_runtime.h:497 | 67-02 | H-severity runtime macro OOM — lands with iron_oom_abort helper |
| 2 | src/runtime/iron_runtime.h:640-641 | 67-02 | H-severity runtime macro OOM — same helper |
| 3 | src/lir/emit_c.c:3159-3168 | 67-02 | H-severity generated-C OOM — emit iron_oom_abort call in codegen |
| 4 | src/lir/emit_c.c:3179-3192 | 67-02 | H-severity generated-C OOM — same |
| 14 | src/comptime/comptime.c:410-426 | 67-03 | Integer-overflow arithmetic — __builtin_add/sub/mul_overflow |
| 15 | src/comptime/comptime.c:493 | 67-03 | INT64_MIN negation — explicit check |
| 19 | src/parser/parser.c:2585 | 67-03 | atoi → strtol for enum variant values |

### FIX-02 full walkthrough (non-top-20 arena sites)

The full 285 M-severity arena sites enumerated in CORRECTNESS-AUDIT.md §6 (Allocation Error Handling) are NOT in the top-20 table but are in-scope for Phase 67 per 67-CONTEXT.md LOCKED decision. Those sites are distributed across plans 67-04 (parser), 67-05 (analyzer + comptime), 67-06 (hir + lir + runtime/stdlib) by subsystem ownership.

### FIX-03 cross-arena rows (AUDIT-04 16 M rows)

All 16 AUDIT-04 M-severity rows are assigned to plan 67-07 (FIX-03 focus plan).

### FIX-04 enum-switch tail (AUDIT-02 17 M rows)

Most AUDIT-02 M rows are already covered by Phase 66-02's -Werror=switch-enum rollout (82 opt-out comments landed). Any that still lack an explicit case or opt-out comment are assigned to plan 67-03 (FIX-04 tail).

### REG-02 canaries (15 fixtures)

All 15 canary fixtures are assigned to plan 67-08 (last plan, runs after all fixes land).

---
*Phase: 67-correctness-fixes-+-crash-canaries*
*Status verification: 2026-04-13*
```

**For each DONE row**, verify by running the grep listed in the Evidence column on the live source — if a grep claim does not match reality, mark the row OPEN and reassign it to a plan. Do NOT rely solely on the Phase 66 SUMMARY text — verify against current head.

**For the Wasm re-audit**, run each grep dimension listed above against each of the 6 Wasm files. For any blind cast found without a preceding `IRON_NODE_ASSERT_KIND` or kind check, add a row with severity H (if the cast is in hot user code paths) or M (if it's in cold init code). Expected result: 0 H-severity rows because those files were written after Phase 66-02 landed `-Werror=switch-enum` and after PROT-04 established the kind-guard idiom — any blind cast introduced afterwards would have built-broken against the new CI flags. If you find H rows, add them to the FIX-01 work list and assign to plan 67-02 or 67-03 as appropriate.

**No code changes in this plan.** Do not touch any source file. The deliverable is `.planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` only.
  </action>
  <verify>
    <automated>test -f .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md &amp;&amp; grep -c "^| " .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md | awk '$1 >= 20 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` exits 0
    - `grep -c "^| [0-9]* |" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` returns at least 20 (top-20 table rows)
    - `grep -c "OPEN" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` returns at least 7 (the 7 open rows)
    - `grep -c "DONE" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` returns at least 13 (the 13 resolved rows)
    - `grep -c "Wasm Re-Audit" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` returns exactly 1
    - `grep -c "Plan Assignment" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` returns exactly 1
    - `grep -c "src/lir/emit_web.c" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` returns at least 1 (Wasm file listed in re-audit)
    - `grep -c "67-02\|67-03" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` returns at least 7 (plan assignments for OPEN rows)
    - No source files modified: `git status --short src/ tests/` returns empty
    - Every DONE row evidence includes either a commit SHA (7-character hex) or a grep verification claim
  </acceptance_criteria>
  <done>67-AUDIT-STATUS.md exists with top-20 table (20 rows, 13 DONE / 7 OPEN), Wasm re-audit section with 0 or more new H rows enumerated across all 6 Wasm files, and plan assignment table mapping every OPEN row to its target plan (67-02 through 67-08). Zero source edits. Zero modifications to `src/` or `tests/`.</done>
</task>

</tasks>

<verification>
Phase-level gate: `test -f .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` → file exists; `git status --short src/ tests/` → empty (no code edits); `grep -c "^| [0-9]* |" .planning/phases/67-correctness-fixes-+-crash-canaries/67-AUDIT-STATUS.md` → ≥ 20; every OPEN row has a target plan assignment in the "Plan Assignment" table.
</verification>

<success_criteria>
- 67-AUDIT-STATUS.md exists in the phase directory
- Top-20 table enumerates 20 rows (13 DONE from Phase 66, 7 OPEN)
- Wasm re-audit section covers all 6 post-merge Wasm files
- Plan assignment table maps every OPEN row to 67-02 / 67-03 / 67-04 / 67-05 / 67-06 / 67-07 / 67-08
- Zero changes to `src/`, `tests/`, or any other source tree directory
- Every Phase 67 downstream plan can read this document to know which rows it owns
</success_criteria>

<output>
After completion, create `.planning/phases/67-correctness-fixes-+-crash-canaries/67-01-SUMMARY.md` documenting the verification results: open row count, Wasm re-audit finding count, and the plan assignment distribution (e.g., "67-02: 4 rows, 67-03: 3 rows, 67-04..07: FIX-02 walkthrough + FIX-03 + FIX-04 tail").
</output>
