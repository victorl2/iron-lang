---
phase: 19-lir-rename-hir-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lir/lir.h
  - src/lir/lir.c
  - src/lir/lir_optimize.h
  - src/lir/lir_optimize.c
  - src/lir/lower.h
  - src/lir/lower.c
  - src/lir/lower_internal.h
  - src/lir/lower_exprs.c
  - src/lir/lower_stmts.c
  - src/lir/lower_types.c
  - src/lir/emit_c.h
  - src/lir/emit_c.c
  - src/lir/print.h
  - src/lir/print.c
  - src/lir/verify.h
  - src/lir/verify.c
  - src/cli/build.c
  - src/diagnostics/diagnostics.h
  - CMakeLists.txt
  - tests/lir/CMakeLists.txt
  - tests/lir/test_lir_data.c
  - tests/lir/test_lir_emit.c
  - tests/lir/test_lir_lower.c
  - tests/lir/test_lir_optimize.c
  - tests/lir/test_lir_print.c
  - tests/lir/test_lir_verify.c
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "All IronIR_ types, IRON_IR_ constants, and iron_ir_ functions are renamed to IronLIR_, IRON_LIR_, iron_lir_"
    - "src/ir/ directory no longer exists; all source lives in src/lir/"
    - "tests/ir/ directory no longer exists; all tests live in tests/lir/"
    - "Codebase compiles cleanly with zero warnings"
    - "All existing tests pass unchanged (same count, same results)"
  artifacts:
    - path: "src/lir/lir.h"
      provides: "LIR type definitions with IronLIR_ prefix"
      contains: "IronLIR_Module"
    - path: "src/lir/lir.c"
      provides: "LIR constructors with iron_lir_ prefix"
      contains: "iron_lir_module_create"
    - path: "src/diagnostics/diagnostics.h"
      provides: "Renamed error codes IRON_ERR_LIR_*"
      contains: "IRON_ERR_LIR_MISSING_TERMINATOR"
    - path: "CMakeLists.txt"
      provides: "Updated source paths and test subdirectory"
      contains: "src/lir/lir.c"
  key_links:
    - from: "src/cli/build.c"
      to: "src/lir/lower.h"
      via: "#include"
      pattern: '#include "lir/lower.h"'
    - from: "src/cli/build.c"
      to: "src/lir/emit_c.h"
      via: "#include"
      pattern: '#include "lir/emit_c.h"'
    - from: "CMakeLists.txt"
      to: "src/lir/*.c"
      via: "source list"
      pattern: "src/lir/"
---

<objective>
Rename all IR references to LIR (Lower IR) throughout the entire codebase in a single atomic operation.

Purpose: Establish the naming convention that distinguishes the existing low-level SSA/CFG IR from the new high-level HIR that will be built in subsequent plans. This clears namespace for HIR types (IronHIR_*) and makes the architectural layering explicit.

Output: The entire src/ir/ directory moved to src/lir/, all IronIR_/IRON_IR_/iron_ir_ symbols renamed to IronLIR_/IRON_LIR_/iron_lir_, all includes updated, CMake updated, all tests passing.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-lir-rename-hir-foundation/19-CONTEXT.md
@.planning/phases/19-lir-rename-hir-foundation/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move directories and rename all files</name>
  <files>
    src/lir/ (entire directory, 16 files)
    tests/lir/ (entire directory, 8 files + snapshots/)
  </files>
  <read_first>
    src/ir/ir.h
    src/ir/ir.c
    tests/ir/CMakeLists.txt
    CMakeLists.txt
  </read_first>
  <action>
    Execute the directory and file renames using git mv for proper tracking:

    1. Move source directory:
       - `git mv src/ir src/lir`
       - `git mv src/lir/ir.h src/lir/lir.h`
       - `git mv src/lir/ir.c src/lir/lir.c`
       - `git mv src/lir/ir_optimize.h src/lir/lir_optimize.h`
       - `git mv src/lir/ir_optimize.c src/lir/lir_optimize.c`
       (lower.h, lower.c, lower_internal.h, lower_exprs.c, lower_stmts.c, lower_types.c, emit_c.h, emit_c.c, print.h, print.c, verify.h, verify.c keep their filenames -- only the directory changes)

    2. Move test directory:
       - `git mv tests/ir tests/lir`
       - `git mv tests/lir/test_ir_data.c tests/lir/test_lir_data.c`
       - `git mv tests/lir/test_ir_emit.c tests/lir/test_lir_emit.c`
       - `git mv tests/lir/test_ir_lower.c tests/lir/test_lir_lower.c`
       - `git mv tests/lir/test_ir_optimize.c tests/lir/test_lir_optimize.c`
       - `git mv tests/lir/test_ir_print.c tests/lir/test_lir_print.c`
       - `git mv tests/lir/test_ir_verify.c tests/lir/test_lir_verify.c`
       (snapshots/ directory and CMakeLists.txt keep their names, just the parent dir changes)

    3. Do NOT rename any symbols yet -- this task is file/directory moves only.
  </action>
  <verify>
    <automated>ls src/lir/lir.h src/lir/lir.c src/lir/lir_optimize.h src/lir/lir_optimize.c tests/lir/test_lir_data.c tests/lir/test_lir_emit.c && test ! -d src/ir && test ! -d tests/ir</automated>
  </verify>
  <acceptance_criteria>
    - src/ir/ directory does not exist
    - tests/ir/ directory does not exist
    - src/lir/ contains all 16 source files with correct names (lir.h, lir.c, lir_optimize.h, lir_optimize.c, lower.h, lower.c, lower_internal.h, lower_exprs.c, lower_stmts.c, lower_types.c, emit_c.h, emit_c.c, print.h, print.c, verify.h, verify.c)
    - tests/lir/ contains CMakeLists.txt, snapshots/, and 6 test files with test_lir_* names
  </acceptance_criteria>
  <done>All files moved to src/lir/ and tests/lir/ with correct names; old directories gone</done>
</task>

<task type="auto">
  <name>Task 2: Rename all symbols, includes, and CMake references</name>
  <files>
    src/lir/lir.h
    src/lir/lir.c
    src/lir/lir_optimize.h
    src/lir/lir_optimize.c
    src/lir/lower.h
    src/lir/lower.c
    src/lir/lower_internal.h
    src/lir/lower_exprs.c
    src/lir/lower_stmts.c
    src/lir/lower_types.c
    src/lir/emit_c.h
    src/lir/emit_c.c
    src/lir/print.h
    src/lir/print.c
    src/lir/verify.h
    src/lir/verify.c
    src/cli/build.c
    src/diagnostics/diagnostics.h
    CMakeLists.txt
    tests/lir/CMakeLists.txt
    tests/lir/test_lir_data.c
    tests/lir/test_lir_emit.c
    tests/lir/test_lir_lower.c
    tests/lir/test_lir_optimize.c
    tests/lir/test_lir_print.c
    tests/lir/test_lir_verify.c
  </files>
  <read_first>
    src/lir/lir.h
    src/lir/lir.c
    src/lir/lir_optimize.h
    src/cli/build.c
    src/diagnostics/diagnostics.h
    CMakeLists.txt
    tests/lir/CMakeLists.txt
  </read_first>
  <action>
    Apply three global symbol renames across ALL files in src/lir/, tests/lir/, src/cli/build.c, src/diagnostics/diagnostics.h, and CMakeLists.txt:

    **Symbol renames (applied via sed or manual find-replace):**
    1. `IronIR_` -> `IronLIR_` (type names, typedef names)
    2. `IRON_IR_` -> `IRON_LIR_` (enum constants, macro names, header guards, error codes)
    3. `iron_ir_` -> `iron_lir_` (function names)

    **Include path renames (all files):**
    4. `"ir/ir.h"` -> `"lir/lir.h"`
    5. `"ir/ir_optimize.h"` -> `"lir/lir_optimize.h"`
    6. `"ir/lower.h"` -> `"lir/lower.h"`
    7. `"ir/lower_internal.h"` -> `"lir/lower_internal.h"`
    8. `"ir/emit_c.h"` -> `"lir/emit_c.h"`
    9. `"ir/print.h"` -> `"lir/print.h"`
    10. `"ir/verify.h"` -> `"lir/verify.h"`

    **Header guards:**
    11. `IRON_IR_H` -> `IRON_LIR_H` in lir.h
    12. `IRON_IR_OPTIMIZE_H` -> `IRON_LIR_OPTIMIZE_H` in lir_optimize.h
    13. `IRON_IR_LOWER_H` -> `IRON_LIR_LOWER_H` in lower.h
    14. `IRON_IR_LOWER_INTERNAL_H` -> `IRON_LIR_LOWER_INTERNAL_H` in lower_internal.h
    15. `IRON_IR_EMIT_C_H` -> `IRON_LIR_EMIT_C_H` in emit_c.h
    16. `IRON_IR_PRINT_H` -> `IRON_LIR_PRINT_H` in print.h
    17. `IRON_IR_VERIFY_H` -> `IRON_LIR_VERIFY_H` in verify.h

    **Error codes in diagnostics.h:**
    18. `IRON_ERR_IR_MISSING_TERMINATOR` -> `IRON_ERR_LIR_MISSING_TERMINATOR`
    19. `IRON_ERR_IR_INVALID_BRANCH_TARGET` -> `IRON_ERR_LIR_INVALID_BRANCH_TARGET`
    20. `IRON_ERR_IR_USE_BEFORE_DEF` -> `IRON_ERR_LIR_USE_BEFORE_DEF`
    21. `IRON_ERR_IR_INSTR_AFTER_TERMINATOR` -> `IRON_ERR_LIR_INSTR_AFTER_TERMINATOR`
    22. `IRON_ERR_IR_NO_ENTRY_BLOCK` -> `IRON_ERR_LIR_NO_ENTRY_BLOCK`
    23. `IRON_ERR_IR_RETURN_TYPE_MISMATCH` -> `IRON_ERR_LIR_RETURN_TYPE_MISMATCH`

    **CMakeLists.txt (root):**
    24. Change all `src/ir/` paths to `src/lir/` in the iron_compiler source list
    25. Change `src/ir/ir.c` -> `src/lir/lir.c` and `src/ir/ir_optimize.c` -> `src/lir/lir_optimize.c`
    26. Change `add_subdirectory(tests/ir)` -> `add_subdirectory(tests/lir)`

    **tests/lir/CMakeLists.txt:**
    27. Rename all test targets: `test_ir_data` -> `test_lir_data`, etc.
    28. Update source file names: `test_ir_data.c` -> `test_lir_data.c`, etc.
    29. Update LABELS from `"ir"` to `"lir"`

    **CRITICAL: Do NOT rename:**
    - User-facing CLI flag strings like `"--dump-ir-passes"` or `"--no-optimize"` (these are stable CLI API)
    - Comment text that refers to general "IR" concepts (only rename identifiers)
    - `IRON_ERR_LOWER_*` codes (already correct prefix)
    - Snapshot file contents (they contain printed IR output; update if they reference IronIR_ symbols in their expected output)

    **Post-rename validation:** After all renames, grep the entire src/lir/, tests/lir/, src/cli/build.c, src/diagnostics/diagnostics.h for any remaining `IronIR_`, `IRON_IR_`, or `iron_ir_` occurrences. There must be ZERO hits (except in comments describing the rename or in string literals for CLI flags).

    Build and test:
    ```bash
    cd build && cmake .. && cmake --build . 2>&1
    ctest --output-on-failure -j4
    ```
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang/build && cmake .. && cmake --build . 2>&1 | tail -5 && ctest --output-on-failure -j4 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "IronIR_" src/lir/ src/cli/build.c src/diagnostics/diagnostics.h tests/lir/` returns zero matches (excluding comments about the rename and CLI flag strings)
    - `grep -r "IRON_IR_" src/lir/ src/cli/build.c src/diagnostics/diagnostics.h tests/lir/` returns zero matches (excluding CLI flag strings like --dump-ir-passes)
    - `grep -r "iron_ir_" src/lir/ src/cli/build.c tests/lir/` returns zero matches
    - `grep -r '"ir/' src/lir/ src/cli/build.c tests/lir/` returns zero matches (all includes updated)
    - `cmake --build build` succeeds with zero warnings
    - `ctest --test-dir build --output-on-failure -j4` all tests pass (same count as before rename)
  </acceptance_criteria>
  <done>Every IronIR_/IRON_IR_/iron_ir_ symbol renamed to IronLIR_/IRON_LIR_/iron_lir_; all includes point to lir/; CMake updated; full build + all tests green</done>
</task>

</tasks>

<verification>
1. `cmake --build build` compiles with zero errors and zero warnings
2. `ctest --test-dir build --output-on-failure -j4` -- all tests pass (same count as before)
3. `grep -rn "IronIR_\|IRON_IR_\|iron_ir_" src/lir/ tests/lir/ src/cli/build.c src/diagnostics/diagnostics.h` -- zero hits (excluding CLI flag strings)
4. `test ! -d src/ir && test ! -d tests/ir` -- old directories gone
</verification>

<success_criteria>
- The codebase compiles and all tests pass with the new LIR naming
- No IronIR_/IRON_IR_/iron_ir_ symbols remain in renamed files
- src/ir/ and tests/ir/ directories are gone
- The rename is a single atomic commit with zero behavior change
</success_criteria>

<output>
After completion, create `.planning/phases/19-lir-rename-hir-foundation/19-01-SUMMARY.md`
</output>
