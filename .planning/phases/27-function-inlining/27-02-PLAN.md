---
phase: 27-function-inlining
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified: []
autonomous: true
requirements: [INLINE-01, INLINE-02, INLINE-03]

must_haves:
  truths:
    - "Full integration test suite passes with no regressions from function inlining"
    - "Algorithm tests including connected_components pass with correct output"
    - "Composite tests pass with correct output"
    - "find_root calls in connected_components are inlined (verifiable via --dump-ir-passes)"
  artifacts: []
  key_links:
    - from: "run_function_inlining"
      to: "connected_components find_root calls"
      via: "inlining replaces CALL with cloned body"
      pattern: "After function-inlining.*no.*find_root.*CALL"
---

<objective>
Full test suite validation confirming function inlining introduces no regressions.

Purpose: Prove that inlining is correct across the entire test corpus, not just targeted tests. Verify the primary benchmark target (find_root in connected_components) is actually inlined.

Output: Validated test results confirming all requirements are met.
</objective>

<execution_context>
@/Users/victor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-function-inlining/27-RESEARCH.md
@.planning/phases/27-function-inlining/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression test suite</name>
  <files></files>
  <read_first>
    - .planning/phases/27-function-inlining/27-01-SUMMARY.md (understand what was implemented)
    - .planning/phases/27-function-inlining/27-VALIDATION.md (test commands)
  </read_first>
  <action>
Run the complete test suite to validate no regressions from function inlining:

1. **Integration tests:** `bash tests/run_tests.sh integration`
   - All tests must pass (the two pre-existing failures from Phase 26 — bug_vla_goto_bypass and concurrent_hash_map — are expected and acceptable).
   - Count passing vs failing. Compare to Phase 26 baseline (166/167 integration, 12/13 algorithms).

2. **Algorithm tests:** `bash tests/run_tests.sh algorithms`
   - `connected_components` is the PRIMARY TARGET — must pass with correct output.
   - All other algorithm tests must pass.

3. **Composite tests:** `bash tests/run_tests.sh composite`
   - All composite tests must pass.

4. **Verify find_root is actually inlined:** Run connected_components with `--dump-ir-passes` and check:
   - After the "After function-inlining" dump, the `count_components` function should NOT contain CALL instructions to `find_root`.
   - The inlined body should be visible as additional blocks in `count_components`.
   
   Command: `./build/ironc tests/benchmarks/problems/connected_components/main.iron --dump-ir-passes -o /tmp/cc_dump 2>/tmp/cc_ir_dump.txt && grep -A5 "After function-inlining" /tmp/cc_ir_dump.txt | grep -c "find_root"` — this count should be 0 or only appear in the function definition, not in CALL instructions within count_components.

5. **Verify pass ordering (INLINE-02):** In the IR dump, confirm that "After function-inlining" appears AFTER "After array-repr" and BEFORE "After copy-prop (iter 0)".

If any test fails that was passing before Plan 01, investigate and fix. These would be regressions introduced by inlining.
  </action>
  <verify>
    <automated>cd /Users/victor/code/iron-lang && bash tests/run_tests.sh integration 2>&1 | tail -3 && bash tests/run_tests.sh algorithms 2>&1 | tail -3 && bash tests/run_tests.sh composite 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Integration tests: same pass/fail ratio as Phase 26 (166/167 or better; only pre-existing failures allowed)
    - Algorithm tests: same pass/fail ratio as Phase 26 (12/13 or better)
    - Composite tests: 3/3 pass
    - connected_components produces correct output (Test 1: 50, Test 2: 1, Test 3: 10, Test 4: 25)
    - Pass ordering confirmed: function-inlining runs after array-repr and before fixpoint
  </acceptance_criteria>
  <done>Full test suite passes with no new regressions; connected_components correct; pass ordering verified</done>
</task>

</tasks>

<verification>
1. Full test suite results match or exceed Phase 26 baseline
2. connected_components benchmark passes
3. Pass ordering confirmed via IR dump
4. All three requirements (INLINE-01, INLINE-02, INLINE-03) validated by passing tests
</verification>

<success_criteria>
- Zero new regressions from function inlining
- Primary benchmark target (find_root) confirmed inlined
- Pass ordering correct (after array-repr, before fixpoint)
- All INLINE-* requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/27-function-inlining/27-02-SUMMARY.md`
</output>
