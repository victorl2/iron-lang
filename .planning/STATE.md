---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: milestone
status: in_progress
stopped_at: Completed 63-02-PLAN.md
last_updated: "2026-04-16T22:57:47Z"
last_activity: "2026-04-16 — Phase 63 Plan 02 executed on local. 17 Iron_draw_* prototypes + 17 shim implementations + 17 Iron func Draw.* stubs added for pixel/line/circle/ellipse/ring primitives. Vector2-by-value INPUT ABI validated across 10 shim sites — first-ever validation of this ABI direction. DrawCircleGradient two-Color variant bound via double memcpy. clang -c iron_raylib.c and iron_raylib_layout.c both exit 0 with zero warnings on first compile. DRAW2D-07, 09, 10, 11 closed. DRAW2D-08 4/5 (DrawLineStrip deferred to Plan 63-04 pending array ABI probe)."
progress:
  total_phases: 14
  completed_phases: 2
  total_plans: 20
  completed_plans: 18
  percent: 90
---

# State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-13)

**Core value:** Every Iron feature compiles to correct, working C code; the stdlib exposes raylib in idiomatic Iron form.
**Current focus:** v2.0.0-alpha — Iron Builds Real Games (close all raylib gaps)

## Current Position

Phase: 63 — 2D Drawing (IN PROGRESS)
Plan: 03 of 4 — NEXT
Status: Phase 63 Plan 02 COMPLETE (17 Draw.* pixel/line/circle/ellipse/ring stubs/shims landed; Vector2-by-value INPUT ABI validated across 10 shim sites — first-ever validation of this ABI direction; DrawCircleGradient two-Color variant bound via double memcpy; DRAW2D-07/09/10/11 closed; DRAW2D-08 partially closed — 4 of 5 line primitives bound; DrawLineStrip deferred to Plan 63-04 pending [Vector2] array ABI probe). Phase 63 Plan 01 also COMPLETE. Phase 62 COMPLETE (4/4 plans, INPUT-01..13 closed). Phase 61 plans (61-01..04) still on branch without SUMMARY.md files; state tech-debt. Phase 60 remains 8/8 closed. Local synced from silvaserver.local 2026-04-16 via rsync after stale-state rollback; git branch `remote/pr-28` now tracks the remote work tree (44 commits ahead of main: Phase 60/61/62 + Phase 63 Plans 01/02 done + Phase 63 Plans 03/04 planned).
Last activity: 2026-04-16 — Phase 63 Plan 02 executed on local. 17 Iron_draw_* prototypes + 17 shim implementations + 17 Iron func Draw.* stubs added across src/stdlib/iron_raylib.{h,c} and src/stdlib/raylib.iron for pixel/line/circle/ellipse/ring primitives. Vector2-by-value INPUT validated across 10 shims (pixel_v, line_v, line_ex, line_bezier, circle_sector, circle_sector_lines, circle_v, circle_lines_v, ring, ring_lines). Three shims (line_v, line_ex, line_bezier) memcpy TWO Vector2 params. DrawCircleGradient bound with double-Color memcpy. DrawLineStrip intentionally deferred. clang -c iron_raylib.c and iron_raylib_layout.c both exit 0 with zero warnings on first compile — Phase 60 _Static_assert grid held.

Progress: [█████████░] 90%

## Performance Metrics

- Milestone requirements: 183
- Phases defined: 14
- Phases complete: 1 (Phase 60 — Type & Enum Foundation)
- Phases in flight: 3 (Phase 61 — Window & System via git history; Phase 62 — Input COMPLETE pending SUMMARY.md tech-debt; Phase 63 — 2D Drawing, Plans 01 + 02 complete)
- Coverage: 100% (0 unmapped)
- Requirements complete: 70 (API-08, API-09, API-11 override, TYPE-01..32, ENUM-01..22, INPUT-01..13, DRAW2D-01..07, DRAW2D-09..11; DRAW2D-08 partial — 4 of 5 primitives bound)

### Plan execution log

| Plan  | Tasks | Duration | Files | Commits                                     |
| ----- | ----- | -------- | ----- | ------------------------------------------- |
| 60-01 | 5     | ~7 min   | 5     | f46976f, b881d9c, 0e3e5eb, ab94d4a, f87731f |
| 60-02 | 3     | ~3 min   | 3     | 475302b, 237fc65, 819ddfc                   |
| 60-07 | 2     | ~5 min   | 2     | 6a5ebc6, 53d79d5                            |
| 60-03 | 3     | ~3 min   | 3     | 138d97a, facdd29, 1577cea                   |
| 60-04 | 3     | ~3 min   | 3     | f5411b6, cb5fb05, b0531e6                   |
| 60-05 | 3     | ~2 min   | 3     | 090f171, ad43b7a, ebfe8c9                   |
| 60-06 | 2     | ~2 min   | 1     | f83b633, b152dff                            |
| 60-08 | 4     | ~4 min   | 5     | 2164bcd, 5223072, 790e285, 36c060a          |
| 62-01 | 3     | ~4 min   | 4     | 0d14923, d213075, 728046a                   |
| 63-01 | 3     | ~3 min   | 3     | e2739f6, ec632e5, 7768040                   |
| 63-02 | 2     | ~3 min   | 3     | 20b4f53, cc494a2                            |

## Accumulated Context

- `.planning/` was untracked in commit 58404fe (April 2026). Prior PROJECT.md/MILESTONES.md/STATE.md/ROADMAP.md/REQUIREMENTS.md no longer exist on disk. CHANGELOG.md is the authoritative record of shipped work; latest release is v1.2.0-alpha (Networking Foundation, Phase 59, 2026-04-11).
- Phase numbering continues from 59. New phases for this milestone start at Phase 60.
- Iron's raylib binding today is `src/stdlib/raylib.iron` (~68 lines, 18/581 functions, 1/22 enums, 2/20 types — ~3% coverage). Real users (examples/pong, tests/manual/game_raylib) are blocked from any non-trivial game.
- raylib 5.5 vendored sources at `src/vendor/raylib/` are wired into both native (`src/cli/build.c`) and web (`src/cli/build_web.c` via emcc + `-sUSE_GLFW=3`) builds.
- **60-01 (2026-04-14):** `iron_raylib.c` / `iron_raylib.h` / `iron_raylib_layout.c` scaffolds landed as empty TUs with section markers. Both native `build.c` and web `build_web.c` pipelines conditionally compile them when `opts.use_raylib` is true. `ironc` itself does NOT link `iron_raylib.c` (excluded from CMake `iron_stdlib` target) — the shim is end-user compile-time only, avoiding a raylib.h dependency in ironc. pong.iron regression confirmed (compiles cleanly with the new pipeline).
- **60-01 decision:** Shim TUs appended to argv inside the `if (opts.use_raylib && *rl_src_out)` block (conditional), NOT inside the unconditional stdlib block. Rationale: non-raylib builds have no `-Isrc/vendor/raylib` include path.
- **60-01 decision:** Approach A for `build_web.c` — bumped `IRON_WEB_RAYLIB_SRC_COUNT` from 7 to 9 and let the existing alloc/free/emcc-append loops handle the two new entries automatically. Zero loop changes required.
- **60-02 (2026-04-14):** Core math types landed — `raylib.iron` rewritten with 7 `object` declarations (Vector2, Vector3, Vector4, Quaternion, Matrix, Rectangle, Color), `iron_raylib.h` populated with 7 matching `struct Iron_*` mirrors, `iron_raylib_layout.c` populated with 45 `_Static_assert` entries (size + per-field offsetof for every type). Clean break: old Vec2/Color-var/Key enum/color constants/extern funcs all REMOVED. Plans 60-03..07 will extend the same three files incrementally; Plan 60-08 will rewrite pong/hello_raylib/game_raylib consumers.
- **60-02 decision:** `val` (immutable) for every field across all 7 types — default per CONTEXT.md discretion. Revisit per-type only if mutation patterns emerge in later phases.
- **60-02 decision:** Quaternion as a standalone `object` (duplicate Vector4 layout) rather than a typealias — Iron has no type-alias syntax. The `_Static_assert` group pins Iron_Quaternion to both the `Quaternion` typedef name and the concrete `Vector4` struct.
- **60-02 decision:** Relied on direct `clang -c` verification of iron_raylib_layout.c (ABI ground truth) rather than end-to-end `ironc build pong.iron` — the end-to-end path would fail anyway because pong.iron references removed symbols (Plan 60-08 is where that test lives).
- **60-02 note:** pong.iron / hello_raylib.iron / game_raylib.iron are now BROKEN (reference removed Vec2/RED/InitWindow symbols). This is expected per the CONTEXT.md clean-break plan. Do not attempt to run them until Plan 60-08 rewrites them.
- **REQ count discrepancy:** REQUIREMENTS.md header + traceability footer both say "159 total" but the file actually contains **183** explicit REQ-ID entries. ROADMAP.md and Traceability table are built on the actual 183. The `159` figure should be corrected in REQUIREMENTS.md at the next edit pass.
- **60-07 (2026-04-14):** All 21 raylib enums landed — `raylib.iron` extended with 21 `enum` declarations (KeyboardKey/MouseButton/MouseCursor/GamepadButton/GamepadAxis, ConfigFlags/TraceLogLevel/BlendMode, PixelFormat/TextureFilter/TextureWrap/CubemapLayout, FontType, CameraMode/CameraProjection, MaterialMapIndex, ShaderLocationIndex/ShaderUniformDataType/ShaderAttributeDataType, Gesture, NPatchLayout — total ~260 values, KeyboardKey alone is 101 values). `iron_raylib_layout.c` extended with 209 `_Static_assert` anchors pinning every raylib C constant to Iron's hand-copied integer (total static_assert count now 257: 48 Plan 60-02 type asserts + 209 Plan 60-07 enum anchors). Clang compile of iron_raylib_layout.c exits 0 on macOS arm64 — ground truth that every ordinal matches raylib 5.5.
- **60-07 decision:** Anchor-assert strategy (`_Static_assert(KEY_SPACE == 32, "KEY_SPACE drifted")`) chosen over Iron_<Enum>_<Value> symbol asserts. Iron's HIR lowers enum references to inline integer literals (no Iron_KeyboardKey_SPACE C symbol is emitted), so anchoring raylib's C constants is the only ABI-safe verification. Catches any future raylib upgrade that renumbers a constant with a clear `<CONST> drifted` compile error.
- **60-07 decision:** Transcribed 21 enums, not 22 — raylib.h defines 21 `typedef enum` blocks and REQUIREMENTS.md ENUM-01..21 name 21 distinct enum types. ENUM-22 is a cross-cutting meta-requirement ("ordinals must match + all API parameters use typed enums"), not a 22nd enum. The plan document's "22 enums" prose is an internal typo — the per-enum acceptance grep list has 21 entries, the enum table has 21 rows, raylib.h has 21 definitions. ENUM-22's ordinal-match half is satisfied by the 209 anchor asserts; the typed-param half is legitimately deferred to Phases 61+ where function signatures are written.
- **60-07 decision:** KeyboardKey uses a 20-entry representative anchor sample (spanning every section of the 101-value enum: NULL/SPACE/ESCAPE/ENTER/arrows/A/Z/F1/F12/KP_0/KP_EQUAL/LEFT_SHIFT/KB_MENU/Android volume/menu/back) rather than 101 near-identical asserts. All other enums (≤28 values) are fully anchored. The sample catches any section-wide drift without drowning the file in noise.
- **60-07 note:** Out-of-order execution (Wave 2 parallel-safe, dependency only on 60-01). Plans 60-03 through 60-06 have not executed yet; `raylib.iron` still has only the 7 core math types from Plan 60-02 plus the 21 enums appended by Plan 60-07. The file currently does NOT contain Image/Texture/Font/Camera/Mesh/Model/Ray/Audio types — those land in 60-03..05. `iron_raylib.h` still has empty section markers for Plans 60-03..05.
- **60-03 (2026-04-14):** 6 image/texture/font types landed — `raylib.iron` extended with Image, Texture, RenderTexture, NPatchInfo, GlyphInfo, Font (total 13 `object` declarations), `iron_raylib.h` extended with 6 `struct Iron_*` mirrors using nested-by-value embedding and `void *` for opaque pointer fields, `iron_raylib_layout.c` extended with 36 new `_Static_assert` entries (6 size + 30 per-field offsetof). Total assertion count in layout TU is now 293 (48 Plan 60-02 + 209 Plan 60-07 + 36 Plan 60-03). Clang compile exits 0 on macOS arm64.
- **60-03 decision:** Iron `Int32` / `UInt32` / plain `Int` trichotomy for raylib scalar fields — C `int` → `Int32` (4 bytes), C `unsigned int` → `UInt32` (4 bytes), C `void *` / `Type *` → `Int` with `_` prefix (8 bytes, matches int64_t pointer size on every 64-bit target Iron supports). Iron's default `Int` is int64_t which would double every C-int field's size and destroy struct layout. Int32 and UInt32 are registered primitives in src/analyzer/resolve.c line 885-890.
- **60-03 decision:** `format` field in Image/Texture and `layout` field in NPatchInfo stored as Int32, NOT as typed PixelFormat/NPatchLayout enum fields. Iron's enum-as-struct-field pattern is untested elsewhere in the codebase; adopting it would expand Plan 60-03 into language-feature territory. raylib's own C code stores these as `int` and casts to the enum at use-site, so the byte layout is identical. Follow-up cleanup in Phases 66/67 can tighten the Iron type once the enum-field pattern is proven safe in a smaller setting.
- **60-03 decision:** Single Iron `Texture` type covers raylib's `Texture` / `Texture2D` / `TextureCubemap` — all three are plain typedefs for the same C struct with zero layout information. Drop suffixes per 60-CONTEXT.md convention; functions that distinguish cubemaps from 2D textures handle it via parameter/doc text in Phases 66+.
- **60-03 CRITICAL RESULT — nested-object codegen verified layout-safe:** Plan 60-03 was the first plan to define Iron types that embed other Iron `object`s by value (RenderTexture embeds two Textures, NPatchInfo embeds Rectangle, GlyphInfo embeds Image, Font embeds Texture). The `_Static_assert(offsetof(Iron_RenderTexture, texture) == offsetof(RenderTexture, texture))` and equivalent asserts for all 4 nested-embedding cases all passed on the first try. **Iron's codegen handles embedded-by-value `object` fields correctly**, matching C struct-by-value embedding byte-for-byte. Plans 60-04 (Camera2D/Camera3D/Transform/Model embedding Vector2/Vector3/Quaternion/Matrix/Color) and 60-05 (Ray/BoundingBox/RayCollision embedding Vector3) are now explicitly unblocked — the stress-test concern raised in the execution prompt did not materialise.
- **60-04 (2026-04-14):** 10 camera/mesh/material/model types landed — `raylib.iron` extended with Camera, Camera3D, Transform, BoneInfo, Mesh, Shader, MaterialMap, Material, Model, ModelAnimation (total 23 `object` declarations), `iron_raylib.h` extended with 10 `struct Iron_*` mirrors including inline `char name[32]` / `float params[4]` arrays and `struct Iron_Transform **_framePoses` pointer-to-pointer, `iron_raylib_layout.c` extended with 63 new `_Static_assert` entries (10 size + 53 per-field offsetof). Total assertion count in layout TU is now **356** (48 Plan 60-02 + 209 Plan 60-07 + 36 Plan 60-03 + 63 Plan 60-04). Clang compile exits 0 on macOS arm64 on first try.
- **60-04 decision:** Inline char[32] arrays (BoneInfo.name, ModelAnimation.name) flattened to 32 consecutive `val _name_NN: UInt8` fields on the Iron side with the C mirror keeping `char name[32]`. Inline float[4] (Material.params) flattened to 4 consecutive `val _params_N: Float32` fields with C mirror keeping `float params[4]`. One offset assertion on the field following the array pins the array's tail offset implicitly, which (combined with Iron's contiguous UInt8/Float32 layout) proves the whole run is byte-correct. No need for 32 separate per-byte assertions per char array.
- **60-04 decision:** Pointer-to-pointer type (ModelAnimation.framePoses = `Transform **`) stored as a single Iron `val _framePoses: Int` with `_` prefix, same treatment as a regular single-indirect opaque. The C mirror preserves `struct Iron_Transform **_framePoses` for grep-ability but from Iron's perspective every opaque pointer is just an 8-byte Int regardless of indirection depth.
- **60-04 decision:** Iron `Camera` (no suffix) = raylib C `Camera2D`; Iron `Camera3D` = raylib C `Camera3D`/`Camera` typedef. This naming inversion from raylib's C convention (where `typedef Camera3D Camera`) is now LOCKED at compile time via `_Static_assert(sizeof(struct Iron_Camera) == sizeof(Camera2D))` plus the equivalent for Camera3D. Any future patch that accidentally treats Iron Camera as raylib's Camera3D will fail this assertion and break the build.
- **60-04 decision:** Camera3D.projection stored as plain `Int32` in Iron and `int` in the C mirror, not as a `CameraProjection` enum field. Same rationale as Plan 60-03's Image.format/Texture.format/NPatchInfo.layout — Iron's enum-as-struct-field pattern is still untested; raylib's own C code stores it as `int` and casts to the enum at use-site; byte layout is identical; follow-up cleanup in Phase 69 can tighten the Iron type once the enum-field pattern proves safe.
- **60-04 note:** Mesh is the largest single-type layout group in Phase 60 with 18 assertions (1 size + 17 offset). vertexCount/triangleCount are Int32 at offsets 0/4, followed by 14 consecutive opaque pointer fields (14×8=112 bytes), then boneCount (Int32), vaoId (UInt32), and _vboId (Int = pointer to unsigned int array). The C mirror declares each opaque pointer with its raylib pointer type (float*/unsigned char*/unsigned short*/Iron_Matrix*) for grep-ability.
- **60-04 note:** Model is the first type in Phase 60 to embed Matrix by value (64 bytes at offset 0). Plan 60-02 verified Matrix's own 16-float layout but nothing had embedded it until now; the clang compile confirmed Iron's nested codegen handles the Matrix embed correctly inside a larger struct. Transform is the first type after standalone Quaternion to embed Quaternion — the Vector3 + Quaternion + Vector3 layout also verified.
- **60-05 (2026-04-14):** 8 3D-helper/audio/file types landed — `raylib.iron` extended with Ray, RayCollision, BoundingBox, Wave, AudioStream, Sound, Music, FilePathList (total **31** `object` declarations), `iron_raylib.h` extended with 8 matching `struct Iron_*` mirrors using embedded Vector3 + `void *` opaque pointers for rAudioBuffer*/rAudioProcessor*/void *ctxData/char **, `iron_raylib_layout.c` extended with 36 new `_Static_assert` entries (8 size + 28 per-field offsetof). Total assertion count in layout TU is now **392** (356 Plan 60-04 carry-forward + 36 Plan 60-05). Clang compile exits 0 on macOS arm64 on first try. **All 30 non-namespace raylib types from TYPE-01..30 are now defined.**
- **60-05 CRITICAL RESULT — Iron Bool struct-field ABI verified compatible with C bool:** Plan 60-05 was the first plan to put an Iron `Bool` field inside a struct (RayCollision.hit, Music.looping). If Iron had lowered Bool to anything other than C's stdbool.h `bool` (1 byte), the `offsetof(RayCollision, distance) == 4` assertion would have fired because C places `bool hit` at offset 0 with 3 bytes of padding before `float distance`. The assertion PASSED on first try, proving (a) Iron emits Bool as `bool` (1 byte), and (b) Iron applies the same 4-byte alignment padding before the next field that C does. **The Bool ABI is now permanently compile-time-enforced** via the RayCollision and Music asserts; any future patch that changes Iron's Bool lowering breaks these asserts and the build fails loudly. No additional Bool stress tests required for v2.0.0-alpha.
- **60-05 decision:** AudioStream's `rAudioBuffer *buffer` and `rAudioProcessor *processor` mirrored as `void *` in Iron_AudioStream, NOT as `rAudioBuffer *` / `rAudioProcessor *`. raylib forward-declares these types at raylib.h lines 460-461 without struct bodies (private to raudio.c). Using `void *` avoids importing raylib-internal forward declarations into iron_raylib.h while preserving byte-identical pointer size/alignment (verified by the per-field offset asserts on the sampleRate/sampleSize/channels fields that follow).
- **60-05 decision:** Opaque pointer renames (Iron `_ctxData` ↔ raylib `ctxData`, Iron `_data` ↔ raylib `data`, Iron `_paths` ↔ raylib `paths`) verified via asymmetric offsetof — `offsetof(struct Iron_Music, _ctxData) == offsetof(Music, ctxData)` etc. Cleanly handles the Iron underscore convention while pinning byte-identity across the rename. Same pattern as all prior underscore-prefixed fields from Plans 60-03 and 60-04.
- **60-05 decision:** FilePathList's `char **paths` mirrored as `void *_paths` (not `char **_paths`) on the C side. Same `void *` opaque treatment as AudioStream's rAudioBuffer*/rAudioProcessor*. Users get no Iron-side accessor in this plan; Phase 72's shim helpers will copy the paths array into an Iron Array<String> via strdup on the C side.
- **60-05 note:** Sound (2 fields) and Music (5 fields) both embed AudioStream at offset 0 — the first time two separate Iron types embed the same non-math sub-struct at the same position. Verified by the per-type size asserts plus the `offsetof(stream) == 0` asserts. Extends the embed-by-value pattern to audio types.
- **60-06 (2026-04-14):** TYPE-31 / TYPE-32 audit closed + 4 empty namespace `object`s (`Window`, `Draw`, `Audio`, `Files`) appended to `src/stdlib/raylib.iron`. Audit comment block added at the top of the file (between "Phase 60 scope" header and first data-type section) documenting how the accumulated 392 `_Static_assert` offsetof entries in iron_raylib_layout.c prove TYPE-31 by construction, and enumerating all 28 opaque pointer fields (Kind 1 — user-inspectable, accessors land in Phases 66/67/68/70/71/72 — and Kind 2 — raylib-internal, NO accessors ever: AudioStream._buffer, AudioStream._processor, Music._ctxData). Total `^object ` count in raylib.iron now **35** (31 data types + 4 namespace types). iron_raylib.h and iron_raylib_layout.c **NOT** modified — namespace types have no C struct to mirror. First Phase 60 plan to touch only raylib.iron.
- **60-06 decision:** Audit comment block placed at the TOP of raylib.iron (between "Phase 60 scope" header and first data-type section), NOT at the end. Rationale: block documents a contract applying to every type below it, so readers encounter it before the first `object` declaration. Mirrors net.iron's top-of-file convention notes.
- **60-06 decision:** 4 namespace types placed at the END of raylib.iron (after FilePathList), NOT interleaved with data types. Rationale: namespace types have no C struct mirror, no `_Static_assert` group, no iron_raylib.h presence — keeping them in their own section at the end makes the boundary between "ABI-verified mirror types" and "Iron-only namespace types" visible at a glance.
- **60-06 decision:** NO modifications to iron_raylib.h or iron_raylib_layout.c. Namespace types have no layout to verify. Adding empty `struct Iron_Window;` declarations or bogus size-of-void-pointer asserts would produce dead C code. First Phase 60 plan to touch only raylib.iron.
- **60-06 decision:** TYPE-31 closed by construction (the 392 accumulated offsetof asserts from Plans 60-02..05 already prove field order matches C struct order). TYPE-32 closed by construction (all 28 opaque pointer fields already use the `_`-prefixed convention from Plans 60-03/04/05). Plan 60-06's job was documentation, not fixes — no TYPE-31 violations found, no TYPE-32 violations found.
- **60-06 note:** Phase 60 is now 7/8 complete (60-01..07 done; 60-08 remaining). Plan 60-08 (clean-break rewrite of pong/hello_raylib/game_raylib) is now fully type-unblocked — every raylib type the 3 consumer files need is present in raylib.iron (30 data types + 4 namespace types + Quaternion alias). Requirements TYPE-31 and TYPE-32 are the last two type-system requirements in Phase 60; all 56 Phase 60 requirements (API-08, API-09, TYPE-01..32, ENUM-01..22) are now closed.
- **60-08 (2026-04-14):** Clean-break consumer-file rewrite + API-11 override closure. Added 6-color rescue palette (RED, BLUE, GREEN, WHITE, BLACK, DARKGRAY) as module-level `val` declarations at end of `src/stdlib/raylib.iron` using positional `Color(r, g, b, a)` constructor with UInt8 integer literals. Full 26-color palette remains Phase 66 TEX-14 territory (header comment directs TEX-14 to replace this block wholesale). Rewrote `examples/pong/pong.iron`, `tests/manual/game_raylib.iron`, `tests/integration/web/hello_raylib.iron` with approach B (placeholder main body) — preserves Iron-level declarations (GameState enum, int_to_str helper in pong) intact, replaces main() bodies with type-surface exercisers declaring Vector2, Color, KeyboardKey, GameState, String locals kept alive via `_unused_N` bindings, annotates every removed raylib function call with `-- PHASE 6N:` marker naming the future phase that will restore it. Pong.iron compiles END-TO-END through ironc (2,645,992-byte Mach-O 64-bit arm64 executable, runs to exit 0) — this is the first plan in Phase 60 where the full Iron parser → HIR → type check → analyzer → LIR → emit_c → clang → link-against-raylib → executable pipeline is validated on a real user file. game_raylib.iron and hello_raylib.iron skipped per memory-constraint directive (ironc ~10 GB per invocation, pong is canonical). API-11 override closed in REQUIREMENTS.md: line 240 rewritten with override text, `[x]` checkbox, traceability table row updated to `Phase 60 (Plan 08, override) | Complete`. Phase 60 is now **8/8 plans complete**. Total Phase 60 requirements closed: **57** (TYPE-01..32, ENUM-01..22, API-08, API-09, API-11-override).
- **60-08 decision:** Approach B (placeholder main body) used for ALL THREE consumer files, not just pong. Approach A (comment out each call site) for pong's 147-line body would leave 20+ interleaved commented calls harder to read than a clean placeholder. Approach B also kept all three files uniform — same shape, same annotation style — making Phase 61/62/63's restoration work symmetric.
- **60-08 decision:** Float32 explicit-cast pattern discovered and applied. Iron's float literals default to Float (double); Vector2.x/y are Float32; `Vector2(400.0, 300.0)` fails type-check with "field x expects Float32, got Float". Fix: `Vector2(Float32(400.0), Float32(300.0))`. This pattern will recur in every caller of Vector2/Vector3/Vector4/Rectangle until Phase 73 API-03 sugar (`Vector2.of(x, y)`) lands.
- **60-08 decision:** API-11 override closed in the same plan that executes the clean break, not deferred to a separate docs-only plan. 60-CONTEXT.md explicitly stated "the planner must update REQUIREMENTS.md" pre-execution; the planner did not. Plan 60-08's execution is the natural close point, so updating REQUIREMENTS.md inside this plan's execution scope avoids an orphan Pending Decision carrying into Phase 61.
- **60-08 note:** Pong.iron end-to-end compile is the CANONICAL Phase 60 validation — it's the first and only plan where ironc's full pipeline runs on a real user file. Prior Phase 60 plans (60-02..07) stopped at `clang -c iron_raylib_layout.c` ABI ground-truth. Pong.iron exercises every relevant Phase 60 primitive in a single compile: Vector2 with Float32 casts, 4 Color palette references (BLACK/WHITE/RED/DARKGRAY), 5 KeyboardKey values (SPACE/W/S/UP/DOWN), GameState enum, int_to_str helper, String locals. Successful compile + clean exit from the 2.6 MB binary = the Phase 60 foundation is end-to-end sound.
- **60-08 note:** Phase 60 is COMPLETE. All 8 plans finished. All 57 requirements closed. Next milestone phase is 61 (Window & System); Phases 62, 63, 65, 68, 72 can run in parallel after 61. Pong.iron, game_raylib.iron, and hello_raylib.iron all carry `-- PHASE 6N:` restoration markers giving Phase 61/62/63 a ready-made todo list of uncomment-sites.
- **63-01 (2026-04-16):** 13 Draw.* frame + stack-mode bindings landed — `iron_raylib.h` got 13 `Iron_draw_*` prototypes, `iron_raylib.c` got 13 shim implementations, `raylib.iron` got 13 `func Draw.*` empty-body stubs under a new "-- 2D Drawing (Phase 63) --" banner. Breakdown: 7 void no-arg (begin/end + 5 end_* counterparts), 4 single-struct-input (clear/begin_mode_2d/begin_texture_mode/begin_shader_mode), 1 typed-enum (begin_blend_mode), 1 four-scalar (begin_scissor_mode). clang -c iron_raylib.c and iron_raylib_layout.c BOTH exit 0 with zero warnings on first compile. DRAW2D-01..06 closed.
- **63-01 CRITICAL RESULT — 4 new struct-by-value INPUT ABIs validated first-try:** Color (4 bytes), Camera2D (24 bytes), RenderTexture (44 bytes), Shader (16 bytes) all cross the FFI boundary via `memcpy(&rl, &<iron_param>, sizeof(<raylib_type>))`. Phase 60 `_Static_assert` grid held for every type — zero layout drift. Plans 63-02/03/04 now have a proven Color-by-value template to reuse for every draw primitive (DrawRectangle, DrawCircle, DrawLine, DrawText, ...). The risky plan of Phase 63 landed clean.
- **63-01 decision:** `Draw.end()` keyword-collision probe ran `./build/ironc build` on a `func _Probe.end() {}` fixture. Parser, HIR, analyzer, LIR, and emit_c all passed — only the expected linker error for the empty-body stub (no `Iron__Probe_end` symbol existed) terminated the build. Conclusive proof that `end` is NOT a reserved word. Singular `Draw.end()` name retained across all 13 stubs/shims; no fallback to `Draw.finish()` needed. Open Question 2 from 63-RESEARCH.md resolved.
- **63-01 decision:** Typed `BlendMode` enum parameter follows Phase 62's FFI pattern — Iron declares `mode: BlendMode`, ironc lowers to int32_t at the C boundary, shim casts to `(int)` before passing to BeginBlendMode. User code cannot pass an out-of-range int (enum type constraint), satisfying threat T-63-03 from the plan's threat model.
- **63-01 decision:** 4-scalar `BeginScissorMode` wrapper takes 4 int32_t and casts each to `(int)` inside the shim body. Same pattern as Iron_window_set_position from Phase 61. No Iron-side bounds-check on the scalars — raylib internally clamps negative scissor rects (threat T-63-02 accepted, not mitigated).
- **63-01 decision:** `memcpy(&rl, &<param>, sizeof(<raylib_type>))` uniform pattern used for all 4 struct-inputs to avoid strict-aliasing warnings under `-Wall -Wextra`. Compiler elides the copy for trivially copyable structs. Template carried over from Iron_window_set_icon (Phase 61) and Iron_set_shapes_texture (Phase 62).
- **63-01 note:** No ironc invocation in Tasks 2 or 3 — memory discipline preserved per HANDOFF.md (ironc is ~10 GB/run). End-to-end pong.iron validation is deferred to Plan 63-04. Only Task 1's 1-shot keyword probe ran ironc.
- **63-02 (2026-04-16):** 17 Draw.* pixel/line/circle/ellipse/ring primitive bindings landed — iron_raylib.h got 17 new `Iron_draw_*` prototypes (bringing total to 30), iron_raylib.c got 17 shim implementations (+134 lines), raylib.iron got 17 `func Draw.*` empty-body stubs (+60 lines, total `func Draw.` count now 30). Breakdown: 2 pixel (pixel/pixel_v) + 4 line (line/line_v/line_ex/line_bezier — DrawLineStrip deferred) + 7 circle (circle/circle_sector/circle_sector_lines/circle_gradient/circle_v/circle_lines/circle_lines_v) + 2 ellipse (ellipse/ellipse_lines) + 2 ring (ring/ring_lines). clang -c iron_raylib.c and iron_raylib_layout.c both exit 0 with zero warnings on first compile. DRAW2D-07, 09, 10, 11 closed; DRAW2D-08 4/5 closed.
- **63-02 CRITICAL RESULT — Vector2-by-value INPUT ABI validated across 10 shim sites first-try:** Before Plan 63-02, Phase 60 had proven Vector2 layout (Iron_Vector2 byte-identical to raylib Vector2 via _Static_assert) and Phases 61/62 had used Vector2 only as RETURN values. Plan 63-02 was the first consumer of Vector2 as an INPUT parameter. 10 shims (pixel_v, line_v, line_ex, line_bezier, circle_sector, circle_sector_lines, circle_v, circle_lines_v, ring, ring_lines) use `memcpy(&stack_local, &input_param, sizeof(Vector2))` — 3 of them (line_v, line_ex, line_bezier) memcpy TWO Vector2 params into separate locals. Phase 60 `_Static_assert(sizeof(Iron_Vector2)==sizeof(Vector2))` grid held. Zero layout drift, zero warnings. Phase 66/67/69 primitives that take Vector2/Vector3 INPUT can now reuse this pattern with confidence.
- **63-02 CRITICAL RESULT — two-Color shim variant bound cleanly:** DrawCircleGradient takes two Color params (inner, outer) for radial gradient. Shim `Iron_draw_circle_gradient(cx, cy, r, Iron_Color inner, Iron_Color outer)` uses double memcpy (Color i, o; memcpy(&i, &inner, sizeof(Color)); memcpy(&o, &outer, sizeof(Color))). Compiled clean first try. Future gradient primitives (DrawRectangleGradient in Plan 63-03, DrawRectangleGradientEx) will follow the same pattern.
- **63-02 decision:** Parameter naming on Draw.ring / Draw.ring_lines kept as `inner: Float32` / `outer: Float32` per plan's verbatim template. The plan offered an optional rename to `inner_r` / `outer_r` to mirror the shim-side C names, but Iron parameter names are function-scoped — no collision with Draw.circle_gradient's `inner: Color, outer: Color`. Shorter form preferred; rename can still be applied retrospectively if readers find the inconsistency confusing (requires synchronized updates to Iron stubs + C prototypes + C shim bodies).
- **63-02 decision:** Executed the plan's 17-function per-function grep list verbatim despite the plan's prose saying "16 new / 29 total Iron_draw_". The per-function enumeration (2 pixel + 4 line + 7 circle + 2 ellipse + 2 ring = 17) was always authoritative; the "16"/"29" figures were arithmetic typos. Final counts: 17 new, 30 total Iron_draw_* / func Draw.* — documented as Rule 1 auto-fix in the Plan 63-02 SUMMARY.
- **63-02 decision:** DrawLineStrip left DEFERRED to Plan 63-04 per plan-split contract. Both iron_raylib.h and iron_raylib.c carry a `/* DrawLineStrip deferred to Plan 63-04 (array ABI) */` comment in the line-primitives section so future readers see the gap explicitly. Plan 63-04 opens with an Iron `[Vector2]` array ABI micro-probe before committing to the line_strip shim signature.
- **63-02 decision:** No ironc invocation in either task — memory discipline per HANDOFF.md. Verification via `clang -c iron_raylib.c` + `clang -c iron_raylib_layout.c` only. End-to-end pong.iron validation remains Plan 63-04's territory.

## Phase Dependency Map

```
60 (Types + Enums)  ← foundation; all others depend on it
    ├── 61 (Window & System)
    ├── 62 (Input)
    ├── 63 (2D Draw) ─── 64 (Collision)
    │              └──── 66 (Textures) ─── 67 (Text & Fonts)
    │                              └────── 71 (Shaders)
    ├── 65 (raymath)
    ├── 68 (Audio)          ← independent
    ├── 69 (3D Draw) ─── 70 (Models)
    ├── 72 (File I/O)       ← independent
    └── 73 (API Polish, Showcase, Integration) ← depends on ALL prior phases
```

Phases 61, 62, 65, 68, 72 can run in parallel after 60. 73 runs last as a cross-cutting sweep.

## Pending Decisions

- **API-11 override pending commit:** RESOLVED 2026-04-14 by Plan 60-08. REQUIREMENTS.md line 240 rewritten with override text and `[x]` checkbox; traceability table row updated from `API-11 | Phase 73 | Pending` to `API-11 | Phase 60 (Plan 08, override) | Complete`. The three consumer files (pong.iron, game_raylib.iron, hello_raylib.iron) were rewritten to use canonical Phase 60 names (Vector2, Color via rescue palette, KeyboardKey) with placeholder main bodies annotated with `-- PHASE 6N:` markers for Phase 61/62/63 restoration.
- None blocking. Phase 60 is COMPLETE; Phase 61 (Window & System) can start immediately.

## Blockers

- None.

## Session Continuity

Last session: 2026-04-16T22:57:47Z
Stopped at: Completed 63-02-PLAN.md
Next action: Execute Phase 63 Plan 03 (rectangles + triangles + polygons) via `/gsd:execute-phase 63`. The two major ABI patterns — Color-by-value and Vector2-by-value INPUT — are now proven; Plan 63-03 adds Rectangle-by-value INPUT (Phase 60 `_Static_assert(sizeof(Iron_Rectangle)==sizeof(Rectangle))` already pins it) and multi-Vector2 chains (triangles = 3 Vector2 params). Plan 63-04 closes Phase 63 with the `[Vector2]` array ABI probe (for DrawLineStrip + DrawLineStripConcave + DrawTriangleFan) and pong/hello_raylib/game_raylib re-enablement. Parallel Phases 65/68/72 also still open.
Resume file: .planning/phases/63-2d-drawing/63-02-SUMMARY.md
